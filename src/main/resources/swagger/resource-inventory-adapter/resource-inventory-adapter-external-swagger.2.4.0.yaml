---
swagger: "2.0"
info:
  description: "## Resource Inventory Adapter API\n\n## Version : 2.1 - May 11, 2020"
  version: "2.4.0"
  title: "Resource Inventory Adapter"
host: "localhost:8096"
tags:
- name: "CallbackController"
- name: "CallbackControllerV2"
- name: "DatabaseInitializerController"
- name: "Process"
- name: "ResourceInventoryAdapterController"
- name: "ResourceInventoryAdapterControllerV2"
- name: "Restore"
  description: "the Restore API"
- name: "restore"
schemes:
- "http"
paths:
  /api/resourceInventoryAdapter/v2/getCplId:
    get:
      tags:
      - "ResourceInventoryAdapterControllerV2"
      summary: "Synchronous method to get CPL-ID from NE4."
      description: ""
      operationId: "getCplIdFromNE4"
      parameters:
      - name: "homeId"
        in: "query"
        description: "homeId"
        required: true
        type: "string"
      responses:
        200:
          description: "Data was successfully sent"
          schema:
            $ref: "#/definitions/CplIdDto"
        400:
          description: "Bad request"
  /api/resourceInventoryAdapter/v2/ont:
    patch:
      tags:
      - "ResourceInventoryAdapterControllerV2"
      summary: "Update ONT"
      description: ""
      operationId: "updateOntResourceV2"
      parameters:
      - name: "lineId"
        in: "query"
        description: "LineId"
        required: true
        type: "string"
        pattern: "[A-Z0-9]{3}\\.[A-Z0-9]{1,6}\\.[A-Z0-9]{1,10}"
      - name: "ontConnectivityTestResult"
        in: "query"
        description: "ontConnectivityTestResult"
        required: true
        type: "string"
      - name: "homeId"
        in: "query"
        description: "homeId"
        required: false
        type: "string"
        pattern: "^[A-Z0-9]{7}$"
      responses:
        200:
          description: "PON port is successfully updated."
        400:
          description: "Bad request"
        404:
          description: "PON port is not presented in database."
          schema:
            $ref: "#/definitions/ErrorResponse"
        500:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/resourceInventoryAdapter/v2/ont/accessLine:
    post:
      tags:
      - "ResourceInventoryAdapterControllerV2"
      summary: "Searches Access Line information by criteria"
      description: "Searches the access lines by received criteria. Returns the list\
        \ of lineIds of found lines."
      operationId: "searchLineInformation"
      parameters:
      - in: "body"
        name: "body"
        description: "SearchLineInformationDto"
        required: true
        schema:
          $ref: "#/definitions/SearchAccessLineInformationDto"
      responses:
        200:
          description: "Successfully get"
          schema:
            type: "array"
            items:
              $ref: "#/definitions/AccessLineInformationDto"
        400:
          description: "Bad request"
        500:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/resourceInventoryAdapter/v2/ont/{lineId}:
    put:
      tags:
      - "ResourceInventoryAdapterControllerV2"
      summary: "Asynchronous Request Callback method that changes serial number of\
        \ ONT"
      description: "Sends a notification with result of create ONT resource process\
        \ to the consumer"
      operationId: "changeOntSerialNumber"
      parameters:
      - name: "X-Callback-Id"
        in: "header"
        description: "Unique consumerID used to identify the request consumer within\
          \ FUSE and provide support for maintenance."
        required: false
        type: "string"
      - name: "X-Callback-Url"
        in: "header"
        description: "The provided Rest Interface from the consumer where the expected\
          \ callback is sent to."
        required: true
        type: "string"
      - name: "X-Callback-Correlation-Id"
        in: "header"
        description: "UUID V4 generated identifier. Needs to be unique for each request\
          \ overall all systems. With this the callback URL is identified internally.\
          \ If not provided in request it will be generated by the integration layer\
          \ and will be returned in response. The asynchronous callback will provide\
          \ this ID for correlation of request and callback."
        required: true
        type: "string"
        format: "uuid"
      - name: "X-Callback-Error-Url"
        in: "header"
        description: "On 3Scale or provider errors FUSE will try to deliver an error\
          \ on Error Callback URL to the Consumer."
        required: false
        type: "string"
      - name: "X-Callback-Method"
        in: "header"
        description: "Default method if not specified is POST."
        required: false
        type: "string"
      - name: "businessKey"
        in: "query"
        description: "Process business key"
        required: false
        type: "string"
      - name: "lineId"
        in: "path"
        description: "Line ID"
        required: true
        type: "string"
        pattern: "[A-Z0-9]{3}\\.[A-Z0-9]{1,6}\\.[A-Z0-9]{1,10}"
      - name: "newSerialNumber"
        in: "query"
        description: "new Serial Number"
        required: true
        type: "string"
        pattern: "(^[A-F0-9]{16}$|DEFAULT)"
      responses:
        200:
          description: "successful operation"
          schema:
            $ref: "#/definitions/ProcessDto"
        202:
          description: "Async response. PON port is successfully updated."
        400:
          description: "Bad request"
      x-async-responses:
        callback: "callbackChangeOntSerialNumberV2"
    delete:
      tags:
      - "ResourceInventoryAdapterControllerV2"
      summary: "Asynchronous Request Callback method that performs rollback of the\
        \ ONT resource."
      description: ""
      operationId: "decommissioningOntResourceV2"
      parameters:
      - name: "X-Callback-Url"
        in: "header"
        description: "The provided Rest Interface from the consumer where the expected\
          \ callback is sent to."
        required: true
        type: "string"
      - name: "X-Callback-Correlation-Id"
        in: "header"
        description: "UUID V4 generated identifier. Needs to be unique for each request\
          \ overall all systems. With this the callback URL is identified internally.\
          \ If not provided in request it will be generated by the integration layer\
          \ and will be returned in response. The asynchronous callback will provide\
          \ this ID for correlation of request and callback."
        required: true
        type: "string"
        format: "uuid"
      - name: "X-Callback-Method"
        in: "header"
        description: "Default method if not specified is POST."
        required: false
        type: "string"
      - name: "lineId"
        in: "path"
        description: "lineId"
        required: true
        type: "string"
        pattern: "[A-Z0-9]{3}\\.[A-Z0-9]{1,6}\\.[A-Z0-9]{1,10}"
      - name: "serialNumber"
        in: "query"
        description: "serialNumber"
        required: false
        type: "string"
        pattern: "^[A-F0-9]{16}$"
      - name: "rollbackToReservation"
        in: "query"
        description: "rollbackToReservation"
        required: true
        type: "boolean"
      - name: "businessKey"
        in: "query"
        description: "Process business key"
        required: false
        type: "string"
      responses:
        200:
          description: "successful operation"
          schema:
            $ref: "#/definitions/ProcessDto"
        202:
          description: "Immediate response. ONT resource will be tried to roll back."
        400:
          description: "Bad request"
      x-async-responses:
        callback: "callbackDecommissioningOntResourceV2"
  /api/resourceInventoryAdapter/v2/ont/{lineId}/connectivity:
    post:
      tags:
      - "ResourceInventoryAdapterControllerV2"
      summary: "Asynchronous Request Callback method that tests PON port"
      description: ""
      operationId: "testOntResourceV2"
      parameters:
      - name: "X-Callback-Id"
        in: "header"
        description: "Unique consumerID used to identify the request consumer within\
          \ FUSE and provide support for maintenance."
        required: false
        type: "string"
      - name: "X-Callback-Url"
        in: "header"
        description: "The provided Rest Interface from the consumer where the expected\
          \ callback is sent to."
        required: true
        type: "string"
      - name: "X-Callback-Correlation-Id"
        in: "header"
        description: "UUID V4 generated identifier. Needs to be unique for each request\
          \ overall all systems. With this the callback URL is identified internally.\
          \ If not provided in request it will be generated by the integration layer\
          \ and will be returned in response. The asynchronous callback will provide\
          \ this ID for correlation of request and callback."
        required: true
        type: "string"
        format: "uuid"
      - name: "X-Callback-Error-Url"
        in: "header"
        description: "On 3Scale or provider errors FUSE will try to deliver an error\
          \ on Error Callback URL to the Consumer."
        required: false
        type: "string"
      - name: "X-Callback-Method"
        in: "header"
        description: "Default method if not specified is POST."
        required: false
        type: "string"
      - name: "businessKey"
        in: "query"
        description: "Process business key"
        required: false
        type: "string"
      - name: "lineId"
        in: "path"
        description: "LineId"
        required: true
        type: "string"
        pattern: "[A-Z0-9]{3}\\.[A-Z0-9]{1,6}\\.[A-Z0-9]{1,10}"
      responses:
        200:
          description: "successful operation"
          schema:
            $ref: "#/definitions/ProcessDto"
        202:
          description: "Async response. PON port is successfully tested."
        400:
          description: "Bad request"
      x-async-responses:
        callback: "callbackTestOntResourceV2"
  /api/resourceInventoryAdapter/v2/ont/{lineId}/rollback:
    delete:
      tags:
      - "ResourceInventoryAdapterControllerV2"
      summary: "Asynchronous Request Callback method that performs rollback of the\
        \ ONT resource."
      description: ""
      operationId: "rollbackOntResourceV2"
      parameters:
      - name: "X-Callback-Url"
        in: "header"
        description: "The provided Rest Interface from the consumer where the expected\
          \ callback is sent to."
        required: true
        type: "string"
      - name: "X-Callback-Correlation-Id"
        in: "header"
        description: "UUID V4 generated identifier. Needs to be unique for each request\
          \ overall all systems. With this the callback URL is identified internally.\
          \ If not provided in request it will be generated by the integration layer\
          \ and will be returned in response. The asynchronous callback will provide\
          \ this ID for correlation of request and callback."
        required: true
        type: "string"
        format: "uuid"
      - name: "X-Callback-Method"
        in: "header"
        description: "Default method if not specified is POST."
        required: false
        type: "string"
      - name: "lineId"
        in: "path"
        description: "lineId"
        required: true
        type: "string"
        pattern: "[A-Z0-9]{3}\\.[A-Z0-9]{1,6}\\.[A-Z0-9]{1,10}"
      - name: "serialNumber"
        in: "query"
        description: "serialNumber"
        required: false
        type: "string"
        pattern: "^[A-F0-9]{16}$"
      - name: "rollbackToReservation"
        in: "query"
        description: "rollbackToReservation"
        required: true
        type: "boolean"
      - name: "businessKey"
        in: "query"
        description: "Process business key"
        required: false
        type: "string"
      responses:
        200:
          description: "successful operation"
          schema:
            $ref: "#/definitions/ProcessDto"
        202:
          description: "Immediate response. ONT resource will be tried to roll back."
        400:
          description: "Bad request"
      x-async-responses:
        callback: "callbackDecommissioningOntResourceV2"
  /api/resourceInventoryAdapter/v2/ont/{lineId}/state:
    get:
      tags:
      - "ResourceInventoryAdapterControllerV2"
      summary: "Gets ONT state"
      description: "Gets ONT state."
      operationId: "getOntStateV2"
      produces:
      - "application/json"
      parameters:
      - name: "lineId"
        in: "path"
        description: "Line Id"
        required: true
        type: "string"
        pattern: "[A-Z0-9]{3}\\.[A-Z0-9]{1,6}\\.[A-Z0-9]{1,10}"
      responses:
        200:
          description: "Ont Resource is found"
          schema:
            $ref: "#/definitions/OntStateString"
        400:
          description: "Bad request"
        404:
          description: "Serial number is not presented in database."
          schema:
            $ref: "#/definitions/ErrorResponse"
        500:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/resourceInventoryAdapter/v2/reserveAccessLineTask:
    post:
      tags:
      - "ResourceInventoryAdapterControllerV2"
      summary: "Asynchronous Request Callback method that reserves access line by\
        \ home ID"
      description: "Sends request to reserve access line id by home ID."
      operationId: "reserveLineByHomeIdV2"
      parameters:
      - name: "X-Callback-Id"
        in: "header"
        description: "Unique consumerID used to identify the request consumer within\
          \ FUSE and provide support for maintenance."
        required: false
        type: "string"
      - name: "X-Callback-Url"
        in: "header"
        description: "The provided Rest Interface from the consumer where the expected\
          \ callback is sent to."
        required: true
        type: "string"
      - name: "X-Callback-Correlation-Id"
        in: "header"
        description: "UUID V4 generated identifier. Needs to be unique for each request\
          \ overall all systems. With this the callback URL is identified internally.\
          \ If not provided in request it will be generated by the integration layer\
          \ and will be returned in response. The asynchronous callback will provide\
          \ this ID for correlation of request and callback."
        required: true
        type: "string"
        format: "uuid"
      - name: "X-Callback-Error-Url"
        in: "header"
        description: "On 3Scale or provider errors FUSE will try to deliver an error\
          \ on Error Callback URL to the Consumer."
        required: false
        type: "string"
      - name: "X-Callback-Method"
        in: "header"
        description: "Default method if not specified is POST."
        required: false
        type: "string"
      - name: "businessKey"
        in: "query"
        description: "Process business key"
        required: false
        type: "string"
      - name: "homeId"
        in: "query"
        description: "homeId"
        required: true
        type: "string"
      responses:
        200:
          description: "successful operation"
          schema:
            $ref: "#/definitions/ProcessDto"
        202:
          description: "Async response. Reservation request is accepted."
        400:
          description: "Bad request"
      x-async-responses:
        callback: "callbackReserveLineByHomeIdV2"
  /api/resourceInventoryAdapter/v1/accessLine/reservation:
    post:
      tags:
      - "ResourceInventoryAdapterController"
      summary: "Asynchronous Request Callback method that reserves access line by\
        \ home ID"
      description: "Sends request to reserve access line id by home ID."
      operationId: "reserveLineByHomeId"
      parameters:
      - name: "X-Callback-Id"
        in: "header"
        description: "Unique consumerID used to identify the request consumer within\
          \ FUSE and provide support for maintenance."
        required: false
        type: "string"
      - name: "X-Callback-Url"
        in: "header"
        description: "The provided Rest Interface from the consumer where the expected\
          \ callback is sent to."
        required: true
        type: "string"
      - name: "X-Callback-Correlation-Id"
        in: "header"
        description: "UUID V4 generated identifier. Needs to be unique for each request\
          \ overall all systems. With this the callback URL is identified internally.\
          \ If not provided in request it will be generated by the integration layer\
          \ and will be returned in response. The asynchronous callback will provide\
          \ this ID for correlation of request and callback."
        required: true
        type: "string"
        format: "uuid"
      - name: "X-Callback-Error-Url"
        in: "header"
        description: "On 3Scale or provider errors FUSE will try to deliver an error\
          \ on Error Callback URL to the Consumer."
        required: false
        type: "string"
      - name: "X-Callback-Method"
        in: "header"
        description: "Default method if not specified is POST."
        required: false
        type: "string"
      - name: "businessKey"
        in: "query"
        description: "Process business key"
        required: false
        type: "string"
      - name: "homeId"
        in: "query"
        description: "homeId"
        required: true
        type: "string"
      responses:
        200:
          description: "successful operation"
          schema:
            $ref: "#/definitions/ProcessDto"
        202:
          description: "Async response. Reservation request is accepted."
        400:
          description: "Bad request"
      x-async-responses:
        callback: "callbackReserveLineByHomeId"
  /api/resourceInventoryAdapter/v1/ont:
    post:
      tags:
      - "ResourceInventoryAdapterController"
      summary: "Asynchronous Request Callback method that creates PON port"
      description: "Sends a notification with result of create ONT resource process\
        \ to the consumer"
      operationId: "createOntResource"
      consumes:
      - "application/json"
      parameters:
      - name: "X-Callback-Id"
        in: "header"
        description: "Unique consumerID used to identify the request consumer within\
          \ FUSE and provide support for maintenance."
        required: false
        type: "string"
      - name: "X-Callback-Url"
        in: "header"
        description: "The provided Rest Interface from the consumer where the expected\
          \ callback is sent to."
        required: true
        type: "string"
      - name: "X-Callback-Correlation-Id"
        in: "header"
        description: "UUID V4 generated identifier. Needs to be unique for each request\
          \ overall all systems. With this the callback URL is identified internally.\
          \ If not provided in request it will be generated by the integration layer\
          \ and will be returned in response. The asynchronous callback will provide\
          \ this ID for correlation of request and callback."
        required: true
        type: "string"
        format: "uuid"
      - name: "X-Callback-Error-Url"
        in: "header"
        description: "On 3Scale or provider errors FUSE will try to deliver an error\
          \ on Error Callback URL to the Consumer."
        required: false
        type: "string"
      - name: "X-Callback-Method"
        in: "header"
        description: "Default method if not specified is POST."
        required: false
        type: "string"
      - name: "businessKey"
        in: "query"
        description: "Process business key"
        required: false
        type: "string"
      - in: "body"
        name: "body"
        description: "OntRequestDto"
        required: true
        schema:
          $ref: "#/definitions/OntRequestDto"
      responses:
        200:
          description: "successful operation"
          schema:
            $ref: "#/definitions/ProcessDto"
        202:
          description: "Async response. PON port is successfully created."
        400:
          description: "Bad request"
      x-async-responses:
        callback: "callbackCreateOntResource"
    patch:
      tags:
      - "ResourceInventoryAdapterController"
      summary: "Update ONT"
      description: ""
      operationId: "updateOntResource"
      parameters:
      - name: "serialNumber"
        in: "query"
        description: "SerialNumber"
        required: true
        type: "string"
        pattern: "^[A-F0-9]{16}$"
      - name: "ontConnectivityTestResult"
        in: "query"
        description: "ontConnectivityTestResult"
        required: true
        type: "string"
      - name: "homeId"
        in: "query"
        description: "homeId"
        required: false
        type: "string"
        pattern: "^[A-Z0-9]{7}$"
      responses:
        200:
          description: "PON port is successfully updated."
        400:
          description: "Bad request"
        404:
          description: "PON port is not presented in database."
          schema:
            $ref: "#/definitions/ErrorResponse"
        500:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/resourceInventoryAdapter/v1/ont/{serialNumber}:
    put:
      tags:
      - "ResourceInventoryAdapterController"
      summary: "Asynchronous Request Callback method that changes serial number of\
        \ ONT"
      description: "Sends a notification with result of create ONT resource process\
        \ to the consumer"
      operationId: "changeOntSerialNumber"
      parameters:
      - name: "X-Callback-Id"
        in: "header"
        description: "Unique consumerID used to identify the request consumer within\
          \ FUSE and provide support for maintenance."
        required: false
        type: "string"
      - name: "X-Callback-Url"
        in: "header"
        description: "The provided Rest Interface from the consumer where the expected\
          \ callback is sent to."
        required: true
        type: "string"
      - name: "X-Callback-Correlation-Id"
        in: "header"
        description: "UUID V4 generated identifier. Needs to be unique for each request\
          \ overall all systems. With this the callback URL is identified internally.\
          \ If not provided in request it will be generated by the integration layer\
          \ and will be returned in response. The asynchronous callback will provide\
          \ this ID for correlation of request and callback."
        required: true
        type: "string"
        format: "uuid"
      - name: "X-Callback-Error-Url"
        in: "header"
        description: "On 3Scale or provider errors FUSE will try to deliver an error\
          \ on Error Callback URL to the Consumer."
        required: false
        type: "string"
      - name: "X-Callback-Method"
        in: "header"
        description: "Default method if not specified is POST."
        required: false
        type: "string"
      - name: "businessKey"
        in: "query"
        description: "Process business key"
        required: false
        type: "string"
      - name: "serialNumber"
        in: "path"
        description: "Serial Number"
        required: true
        type: "string"
        pattern: "^[A-F0-9]{16}$"
      - name: "newSerialNumber"
        in: "query"
        description: "new Serial Number"
        required: true
        type: "string"
        pattern: "^[A-F0-9]{16}$"
      responses:
        200:
          description: "successful operation"
          schema:
            $ref: "#/definitions/ProcessDto"
        202:
          description: "Async response. PON port is successfully updated."
        400:
          description: "Bad request"
      x-async-responses:
        callback: "callbackCreateOntResource"
    delete:
      tags:
      - "ResourceInventoryAdapterController"
      summary: "Asynchronous Request Callback method that deletes ONT"
      description: ""
      operationId: "decommissioningOntResource"
      parameters:
      - name: "X-Callback-Id"
        in: "header"
        description: "Unique consumerID used to identify the request consumer within\
          \ FUSE and provide support for maintenance."
        required: false
        type: "string"
      - name: "X-Callback-Url"
        in: "header"
        description: "The provided Rest Interface from the consumer where the expected\
          \ callback is sent to."
        required: true
        type: "string"
      - name: "X-Callback-Correlation-Id"
        in: "header"
        description: "UUID V4 generated identifier. Needs to be unique for each request\
          \ overall all systems. With this the callback URL is identified internally.\
          \ If not provided in request it will be generated by the integration layer\
          \ and will be returned in response. The asynchronous callback will provide\
          \ this ID for correlation of request and callback."
        required: true
        type: "string"
        format: "uuid"
      - name: "X-Callback-Error-Url"
        in: "header"
        description: "On 3Scale or provider errors FUSE will try to deliver an error\
          \ on Error Callback URL to the Consumer."
        required: false
        type: "string"
      - name: "X-Callback-Method"
        in: "header"
        description: "Default method if not specified is POST."
        required: false
        type: "string"
      - name: "serialNumber"
        in: "path"
        description: "SerialNumber"
        required: true
        type: "string"
        pattern: "^[A-F0-9]{16}$"
      - name: "businessKey"
        in: "query"
        description: "Process business key"
        required: false
        type: "string"
      responses:
        200:
          description: "successful operation"
          schema:
            $ref: "#/definitions/ProcessDto"
        202:
          description: "Async response. PON port is successfully deleted."
        400:
          description: "Bad request"
      x-async-responses:
        callback: "callbackDeleteOntResource"
  /api/resourceInventoryAdapter/v1/ont/{serialNumber}/connectivity:
    post:
      tags:
      - "ResourceInventoryAdapterController"
      summary: "Asynchronous Request Callback method that tests PON port"
      description: ""
      operationId: "testOntResource"
      parameters:
      - name: "X-Callback-Id"
        in: "header"
        description: "Unique consumerID used to identify the request consumer within\
          \ FUSE and provide support for maintenance."
        required: false
        type: "string"
      - name: "X-Callback-Url"
        in: "header"
        description: "The provided Rest Interface from the consumer where the expected\
          \ callback is sent to."
        required: true
        type: "string"
      - name: "X-Callback-Correlation-Id"
        in: "header"
        description: "UUID V4 generated identifier. Needs to be unique for each request\
          \ overall all systems. With this the callback URL is identified internally.\
          \ If not provided in request it will be generated by the integration layer\
          \ and will be returned in response. The asynchronous callback will provide\
          \ this ID for correlation of request and callback."
        required: true
        type: "string"
        format: "uuid"
      - name: "X-Callback-Error-Url"
        in: "header"
        description: "On 3Scale or provider errors FUSE will try to deliver an error\
          \ on Error Callback URL to the Consumer."
        required: false
        type: "string"
      - name: "X-Callback-Method"
        in: "header"
        description: "Default method if not specified is POST."
        required: false
        type: "string"
      - name: "businessKey"
        in: "query"
        description: "Process business key"
        required: false
        type: "string"
      - name: "serialNumber"
        in: "path"
        description: "SerialNumber"
        required: true
        type: "string"
        pattern: "^[A-F0-9]{16}$"
      responses:
        200:
          description: "successful operation"
          schema:
            $ref: "#/definitions/ProcessDto"
        202:
          description: "Async response. PON port is successfully tested."
        400:
          description: "Bad request"
      x-async-responses:
        callback: "callbackTestOntResource"
  /api/resourceInventoryAdapter/v1/ont/{serialNumber}/rollback:
    delete:
      tags:
      - "ResourceInventoryAdapterController"
      summary: "Asynchronous Request Callback method that performs rollback of the\
        \ ONT resource."
      description: ""
      operationId: "rollbackOntResource"
      parameters:
      - name: "X-Callback-Url"
        in: "header"
        description: "The provided Rest Interface from the consumer where the expected\
          \ callback is sent to."
        required: true
        type: "string"
      - name: "X-Callback-Correlation-Id"
        in: "header"
        description: "UUID V4 generated identifier. Needs to be unique for each request\
          \ overall all systems. With this the callback URL is identified internally.\
          \ If not provided in request it will be generated by the integration layer\
          \ and will be returned in response. The asynchronous callback will provide\
          \ this ID for correlation of request and callback."
        required: true
        type: "string"
        format: "uuid"
      - name: "X-Callback-Method"
        in: "header"
        description: "Default method if not specified is POST."
        required: false
        type: "string"
      - name: "lineId"
        in: "query"
        description: "lineId"
        required: true
        type: "string"
      - name: "serialNumber"
        in: "path"
        description: "serialNumber"
        required: true
        type: "string"
        pattern: "^[A-F0-9]{16}$"
      - name: "rollbackToReservation"
        in: "query"
        description: "rollbackToReservation"
        required: true
        type: "boolean"
      - name: "businessKey"
        in: "query"
        description: "Process business key"
        required: false
        type: "string"
      responses:
        200:
          description: "successful operation"
          schema:
            $ref: "#/definitions/ProcessDto"
        202:
          description: "Immediate response. ONT resource will be tried to roll back."
        400:
          description: "Bad request"
      x-async-responses:
        callback: "callbackRollbackOntResource"
  /api/resourceInventoryAdapter/v1/ont/{serialNumber}/state:
    get:
      tags:
      - "ResourceInventoryAdapterController"
      summary: "Gets ONT state"
      description: "Gets ONT state."
      operationId: "getOntState"
      produces:
      - "application/json;charset=UTF-8"
      parameters:
      - name: "serialNumber"
        in: "path"
        description: "ONT Serial Number"
        required: true
        type: "string"
        pattern: "^[A-F0-9]{16}$"
      responses:
        200:
          description: "Ont Resource is found"
          schema:
            $ref: "#/definitions/OntStateString"
        400:
          description: "Bad request"
        404:
          description: "Serial number is not presented in database."
          schema:
            $ref: "#/definitions/ErrorResponse"
        500:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
  ${openapi.camundaProcessRestore.base-path}/process/{processId}/restore:
    post:
      tags:
      - "restore"
      summary: "Restore process"
      description: "Restore process"
      operationId: "restoreProcess"
      parameters:
      - name: "processId"
        in: "path"
        description: "Process instance id"
        required: true
        type: "string"
        format: "uuid"
      - name: "X-Callback-Correlation-Id"
        in: "header"
        description: "UUID V4 generated identifier. Needs to be unique for each request\
          \ overall all systems. With this the callback URL is identified internally.\
          \ If not provided in request it will be generated by the integration layer\
          \ and will be returned in response. The asynchronous callback will provide\
          \ this ID for correlation of request and callback. "
        required: false
        type: "string"
        format: "uuid"
      responses:
        202:
          description: "Process restore launched"
        400:
          description: "Bad Request"
        404:
          description: "Process instance not found"
  ${openapi.camundaProcessRestore.base-path}/task/{taskId}/restore:
    post:
      tags:
      - "restore"
      summary: "Restore failed task"
      description: "Restore failed task"
      operationId: "restoreTask"
      parameters:
      - name: "taskId"
        in: "path"
        description: "task id (job Id or external task Id)"
        required: true
        type: "string"
        format: "uuid"
      responses:
        202:
          description: "Task restore launched"
        400:
          description: "Bad Request"
        404:
          description: "Task not found"
  /api/ont/v1/process/{processId}/restore:
    post:
      tags:
      - "Process"
      summary: "Restores failed process"
      description: "Restores failed process"
      operationId: "restore"
      parameters:
      - name: "X-Callback-Correlation-Id"
        in: "header"
        description: "UUID V4 generated identifier. Needs to be unique for each request\
          \ overall all systems. With this the callback URL is identified internally.\
          \ If not provided in request it will be generated by the integration layer\
          \ and will be returned in response. The asynchronous callback will provide\
          \ this ID for correlation of request and callback."
        required: false
        type: "string"
        format: "uuid"
      - name: "processId"
        in: "path"
        description: "Process instance id"
        required: true
        type: "string"
        format: "uuid"
      responses:
        200:
          description: "Process restored"
        400:
          description: "Bad Request"
        404:
          description: "Process instance not found"
  /api/test/v1/initialize-database/:
    get:
      tags:
      - "DatabaseInitializerController"
      summary: "Initialize database"
      description: ""
      operationId: "initializeDatabase"
      parameters: []
      responses:
        200:
          description: "Database successfully initialized"
  /callback/resourceInventoryAdapter/v2/changeOntSerialNumber:
    post:
      tags:
      - "CallbackControllerV2"
      summary: "Changes Ont Serial Number callback"
      description: "Sends to a consumer a notification with result of Change ont serial\
        \ number process."
      operationId: "callbackChangeOntSerialNumberV2"
      consumes:
      - "application/json"
      parameters:
      - name: "X-Callback-Correlation-Id"
        in: "header"
        description: "Correlation Id"
        required: true
        type: "string"
        format: "uuid"
      - in: "body"
        name: "body"
        description: "OntChangeResultV2"
        required: true
        schema:
          $ref: "#/definitions/OntChangeResultV2"
      responses:
        200:
          description: "Ont serial number successfully changed"
        400:
          description: "Bad Request (Sync Response on POST Request)"
      x-async-responses:
        reference-operation-id: "changeOntSerialNumberV2"
  /callback/resourceInventoryAdapter/v2/decommissioningOntResource:
    post:
      tags:
      - "CallbackControllerV2"
      summary: "ONT Decommissioning callback"
      description: ""
      operationId: "callbackDecommissioningOntResourceV2"
      consumes:
      - "application/json"
      parameters:
      - name: "X-Callback-Correlation-Id"
        in: "header"
        description: "UUID V4 generated identifier. Needs to be unique for each request\
          \ overall all systems. With this the callback URL is identified internally.\
          \ If not provided in request it will be generated by the integration layer\
          \ and will be returned in response. The asynchronous callback will provide\
          \ this ID for correlation of request and callback."
        required: true
        type: "string"
        format: "uuid"
      - in: "body"
        name: "body"
        description: "DecommissioningResultV2"
        required: true
        schema:
          $ref: "#/definitions/DecommissioningResultV2"
      responses:
        200:
          description: "OK response to decommissioning callback"
        400:
          description: "Bad Request (Sync Response on POST Request)"
      x-async-responses:
        reference-operation-id: "decommissioningOntResourceV2"
  /callback/resourceInventoryAdapter/v2/reserveAccessLineTask:
    post:
      tags:
      - "CallbackControllerV2"
      summary: "Reserves access line by home ID callback"
      description: "Sends response of reservation access line by home ID."
      operationId: "callbackReserveLineByHomeIdV2"
      consumes:
      - "application/json"
      parameters:
      - name: "X-Callback-Correlation-Id"
        in: "header"
        description: "UUID V4 generated identifier. Needs to be unique for each request\
          \ overall all systems. With this the callback URL is identified internally.\
          \ If not provided in request it will be generated by the integration layer\
          \ and will be returned in response. The asynchronous callback will provide\
          \ this ID for correlation of request and callback."
        required: true
        type: "string"
        format: "uuid"
      - in: "body"
        name: "body"
        description: "ReserveLineByHomeIdResultV2"
        required: true
        schema:
          $ref: "#/definitions/ReserveLineByHomeIdResultV2"
      responses:
        200:
          description: "Access line is successfully received"
        400:
          description: "Bad Request (Sync Response on POST Request)"
      x-async-responses:
        reference-operation-id: "reserveLineByHomeIdV2"
  /callback/resourceInventoryAdapter/v2/rollbackOntResource:
    post:
      tags:
      - "CallbackControllerV2"
      summary: "ONT Rollback callback"
      description: ""
      operationId: "callbackRollbackOntResourceV2"
      consumes:
      - "application/json"
      parameters:
      - name: "X-Callback-Correlation-Id"
        in: "header"
        description: "UUID V4 generated identifier. Needs to be unique for each request\
          \ overall all systems. With this the callback URL is identified internally.\
          \ If not provided in request it will be generated by the integration layer\
          \ and will be returned in response. The asynchronous callback will provide\
          \ this ID for correlation of request and callback."
        required: true
        type: "string"
        format: "uuid"
      - in: "body"
        name: "body"
        description: "OntRollbackResultV2"
        required: true
        schema:
          $ref: "#/definitions/OntRollbackResultV2"
      responses:
        200:
          description: "OK response to rollback callback"
        400:
          description: "Bad Request (Sync Response on POST Request)"
      x-async-responses:
        reference-operation-id: "rollbackOntResourceV2"
  /callback/resourceInventoryAdapter/v2/testOntResource:
    post:
      tags:
      - "CallbackControllerV2"
      summary: "Tests PON port callback"
      description: ""
      operationId: "callbackTestOntResourceV2"
      consumes:
      - "application/json"
      parameters:
      - name: "X-Callback-Correlation-Id"
        in: "header"
        description: "UUID V4 generated identifier. Needs to be unique for each request\
          \ overall all systems. With this the callback URL is identified internally.\
          \ If not provided in request it will be generated by the integration layer\
          \ and will be returned in response. The asynchronous callback will provide\
          \ this ID for correlation of request and callback."
        required: true
        type: "string"
        format: "uuid"
      - in: "body"
        name: "body"
        description: "LineTestResult"
        required: true
        schema:
          $ref: "#/definitions/LineTestResultV2"
      responses:
        200:
          description: "PON port is successfully tested"
        400:
          description: "Bad Request (Sync Response on POST Request)"
      x-async-responses:
        reference-operation-id: "testOntResourceV2"
  /callback/resourceInventoryAdapter/v1/changeOntSerialNumber:
    post:
      tags:
      - "CallbackController"
      summary: "Changes Ont Serial Number callback"
      description: "Sends to a consumer a notification with result of Change ont serial\
        \ number process."
      operationId: "callbackChangeOntSerialNumber"
      consumes:
      - "application/json"
      parameters:
      - name: "X-Callback-Correlation-Id"
        in: "header"
        description: "Correlation Id"
        required: true
        type: "string"
        format: "uuid"
      - in: "body"
        name: "body"
        description: "CommissioningResult"
        required: true
        schema:
          $ref: "#/definitions/OntChangeResult"
      responses:
        200:
          description: "Ont serial number successfully changed"
        400:
          description: "Bad Request (Sync Response on POST Request)"
      x-async-responses:
        reference-operation-id: "changeOntSerialNumber"
  /callback/resourceInventoryAdapter/v1/createOntResource:
    post:
      tags:
      - "CallbackController"
      summary: "Creates PON port callback"
      description: ""
      operationId: "callbackCreateOntResource"
      consumes:
      - "application/json"
      parameters:
      - name: "X-Callback-Correlation-Id"
        in: "header"
        description: "UUID V4 generated identifier. Needs to be unique for each request\
          \ overall all systems. With this the callback URL is identified internally.\
          \ If not provided in request it will be generated by the integration layer\
          \ and will be returned in response. The asynchronous callback will provide\
          \ this ID for correlation of request and callback."
        required: true
        type: "string"
        format: "uuid"
      - in: "body"
        name: "body"
        description: "CommissioningResult"
        required: true
        schema:
          $ref: "#/definitions/CommissioningResult"
      responses:
        200:
          description: "PON port is successfully created"
        400:
          description: "Bad Request (Sync Response on POST Request)"
      x-async-responses:
        reference-operation-id: "createOntResource"
  /callback/resourceInventoryAdapter/v1/deleteOntResource:
    post:
      tags:
      - "CallbackController"
      summary: "Decommissioning PON port callback"
      description: ""
      operationId: "callbackDeleteOntResource"
      consumes:
      - "application/json"
      parameters:
      - name: "X-Callback-Correlation-Id"
        in: "header"
        description: "UUID V4 generated identifier. Needs to be unique for each request\
          \ overall all systems. With this the callback URL is identified internally.\
          \ If not provided in request it will be generated by the integration layer\
          \ and will be returned in response. The asynchronous callback will provide\
          \ this ID for correlation of request and callback."
        required: true
        type: "string"
        format: "uuid"
      - in: "body"
        name: "body"
        description: "DecommissioningResult"
        required: true
        schema:
          $ref: "#/definitions/DecommissioningResult"
      responses:
        200:
          description: "PON port is successfully deleted"
        400:
          description: "Bad Request (Sync Response on POST Request)"
      x-async-responses:
        reference-operation-id: "deleteOntResource"
  /callback/resourceInventoryAdapter/v1/error:
    post:
      tags:
      - "CallbackController"
      summary: "Endpoint to receive error from 3scale in case of technical failure"
      description: "Receives error from 3scale in case of technical failure. The endpoint\
        \ is used only for unexpected errors which could not be processed by the provider\
        \ itself. Like HTTP 404 Not Found, HTTP 503 Service Unavailable."
      operationId: "receiveError"
      parameters:
      - name: "X-Callback-Correlation-Id"
        in: "header"
        description: "UUID V4 generated identifier. Needs to be unique for each request\
          \ overall all systems. With this the callback URL is identified internally.\
          \ If not provided in request it will be generated by the integration layer\
          \ and will be returned in response. The asynchronous callback will provide\
          \ this ID for correlation of request and callback."
        required: true
        type: "string"
        format: "uuid"
      - name: "X-Callback-Request-Id"
        in: "header"
        description: "Unique consumerID from request."
        required: true
        type: "string"
        format: "uuid"
      - name: "X-Callback-Error-Id"
        in: "header"
        description: "UUID V4 generated error identifier."
        required: true
        type: "string"
        format: "uuid"
      - name: "X-Callback-Error-HTTP-Code"
        in: "header"
        description: "Http error code such as 404, 503."
        required: true
        type: "string"
      - in: "body"
        name: "body"
        required: false
        schema:
          type: "object"
      responses:
        202:
          description: "Your callback API implementation should return this HTTP status\
            \ code if the data was received successfully"
  /callback/resourceInventoryAdapter/v1/reserveLineByHomeId:
    post:
      tags:
      - "CallbackController"
      summary: "Reserves access line by home ID callback"
      description: "Sends response of reservation access line by home ID."
      operationId: "callbackReserveLineByHomeId"
      consumes:
      - "application/json"
      parameters:
      - name: "X-Callback-Correlation-Id"
        in: "header"
        description: "UUID V4 generated identifier. Needs to be unique for each request\
          \ overall all systems. With this the callback URL is identified internally.\
          \ If not provided in request it will be generated by the integration layer\
          \ and will be returned in response. The asynchronous callback will provide\
          \ this ID for correlation of request and callback."
        required: true
        type: "string"
        format: "uuid"
      - in: "body"
        name: "body"
        description: "ReserveLineByHomeIdResult"
        required: true
        schema:
          $ref: "#/definitions/ReserveLineByHomeIdResult"
      responses:
        200:
          description: "Access line is successfully received"
        400:
          description: "Bad Request (Sync Response on POST Request)"
      x-async-responses:
        reference-operation-id: "reserveLineByHomeId"
  /callback/resourceInventoryAdapter/v1/rollbackOntResource:
    post:
      tags:
      - "CallbackController"
      summary: "ONT Rollback callback"
      description: ""
      operationId: "callbackRollbackOntResource"
      consumes:
      - "application/json"
      parameters:
      - name: "X-Callback-Correlation-Id"
        in: "header"
        description: "UUID V4 generated identifier. Needs to be unique for each request\
          \ overall all systems. With this the callback URL is identified internally.\
          \ If not provided in request it will be generated by the integration layer\
          \ and will be returned in response. The asynchronous callback will provide\
          \ this ID for correlation of request and callback."
        required: true
        type: "string"
        format: "uuid"
      - in: "body"
        name: "body"
        description: "OntRollbackResult"
        required: true
        schema:
          $ref: "#/definitions/OntRollbackResult"
      responses:
        200:
          description: "OK response to rollback callback"
        400:
          description: "Bad Request (Sync Response on POST Request)"
      x-async-responses:
        reference-operation-id: "rollbackOntResource"
  /callback/resourceInventoryAdapter/v1/testOntResource:
    post:
      tags:
      - "CallbackController"
      summary: "Tests PON port callback"
      description: ""
      operationId: "callbackTestOntResource"
      consumes:
      - "application/json"
      parameters:
      - name: "X-Callback-Correlation-Id"
        in: "header"
        description: "UUID V4 generated identifier. Needs to be unique for each request\
          \ overall all systems. With this the callback URL is identified internally.\
          \ If not provided in request it will be generated by the integration layer\
          \ and will be returned in response. The asynchronous callback will provide\
          \ this ID for correlation of request and callback."
        required: true
        type: "string"
        format: "uuid"
      - in: "body"
        name: "body"
        description: "LineTestResult"
        required: true
        schema:
          $ref: "#/definitions/LineTestResult"
      responses:
        200:
          description: "PON port is successfully tested"
        400:
          description: "Bad Request (Sync Response on POST Request)"
      x-async-responses:
        reference-operation-id: "testOntResource"
  /process/{processId}/restore:
    post:
      tags:
      - "restore"
      summary: "Restore process"
      description: "Restore process"
      operationId: "restoreProcess"
      parameters:
      - name: "processId"
        in: "path"
        description: "Process instance id"
        required: true
        type: "string"
        format: "uuid"
      - name: "X-Callback-Correlation-Id"
        in: "header"
        description: "UUID V4 generated identifier. Needs to be unique for each request\
          \ overall all systems. With this the callback URL is identified internally.\
          \ If not provided in request it will be generated by the integration layer\
          \ and will be returned in response. The asynchronous callback will provide\
          \ this ID for correlation of request and callback. "
        required: false
        type: "string"
        format: "uuid"
      responses:
        202:
          description: "Process restore launched"
        400:
          description: "Bad Request"
        404:
          description: "Process instance not found"
  /swagger-ui.html:
    get:
      operationId: "getSwagger"
      parameters: []
      responses:
        default:
          description: "successful operation"
  /task/{taskId}/restore:
    post:
      tags:
      - "restore"
      summary: "Restore failed task"
      description: "Restore failed task"
      operationId: "restoreTask"
      parameters:
      - name: "taskId"
        in: "path"
        description: "task id (job Id or external task Id)"
        required: true
        type: "string"
        format: "uuid"
      responses:
        202:
          description: "Task restore launched"
        400:
          description: "Bad Request"
        404:
          description: "Task not found"
definitions:
  AccessLineInformationDto:
    type: "object"
    properties:
      homeId:
        type: "string"
        description: "Home Id"
      ontSerialNumber:
        type: "string"
        description: "ONT serial number"
      subscriberNeProfileState:
        type: "string"
        description: "Subscriber NE profile state"
        enum:
        - "INACTIVE"
        - "ACTIVE"
      portNumber:
        type: "string"
        description: "Port number"
      slotNumber:
        type: "string"
        description: "Slot number"
      endSz:
        type: "string"
        description: "EndSz"
      lineId:
        type: "string"
        description: "Line Id"
  CallbackError:
    type: "object"
    properties:
      message:
        type: "string"
      status:
        type: "integer"
        format: "int32"
      code:
        type: "string"
  CommissioningResult:
    type: "object"
    properties:
      error:
        description: "Result of fail operation."
        $ref: "#/definitions/CallbackError"
      response:
        description: "Result of successful operation."
        $ref: "#/definitions/OntResource"
      success:
        type: "boolean"
    description: "CommissioningResult"
  CplIdDto:
    type: "object"
    properties:
      cplId:
        type: "string"
        description: "Customer Premise Location ID of a GF-TA"
    description: "CplID"
  DecommissioningResult:
    type: "object"
    properties:
      error:
        description: "Result of fail operation."
        $ref: "#/definitions/CallbackError"
      response:
        type: "object"
        description: "Result of successful operation."
      success:
        type: "boolean"
    description: "DecommissioningResult"
  DecommissioningResultV2:
    type: "object"
    properties:
      error:
        description: "Result of fail operation."
        $ref: "#/definitions/CallbackError"
      response:
        type: "object"
        description: "Result of successful operation."
      success:
        type: "boolean"
    description: "DecommissioningResultV2"
  ErrorResponse:
    type: "object"
    properties:
      errorCode:
        type: "string"
        description: "Error code"
      message:
        type: "string"
        description: "Message"
    description: "Error response of 4xx and 5xx http statuses"
  LineIdAndCplIdDto:
    type: "object"
    properties:
      cplId:
        type: "string"
        description: "Customer Premise Location ID of a GF-TA"
      lineId:
        type: "string"
        description: "Line ID"
    description: "LineID and CplID"
  LineIdSerialNumberDto:
    type: "object"
    properties:
      serialNumber:
        type: "string"
      lineId:
        type: "string"
    description: "Encapsulate information about access line and serial number"
  LineTestResult:
    type: "object"
    properties:
      error:
        description: "Result of fail operation."
        $ref: "#/definitions/CallbackError"
      response:
        type: "string"
        description: "Result of successful operation."
      success:
        type: "boolean"
    description: "LineTestResult"
  LineTestResultV2:
    type: "object"
    properties:
      error:
        description: "Result of fail operation."
        $ref: "#/definitions/CallbackError"
      response:
        description: "Result of successful operation."
        $ref: "#/definitions/OntTestDto"
      success:
        type: "boolean"
    description: "LineTestResultV2"
  OntChangeResult:
    type: "object"
    properties:
      error:
        description: "Result of fail operation."
        $ref: "#/definitions/CallbackError"
      response:
        type: "string"
        description: "Result of successful operation."
      success:
        type: "boolean"
    description: "OntChangeResult"
  OntChangeResultV2:
    type: "object"
    properties:
      error:
        description: "Result of fail operation."
        $ref: "#/definitions/CallbackError"
      response:
        description: "Result of successful operation."
        $ref: "#/definitions/LineIdSerialNumberDto"
      success:
        type: "boolean"
    description: "OntChangeResultV2"
  OntRequestDto:
    type: "object"
    required:
    - "lineId"
    properties:
      serialNumber:
        type: "string"
        description: "Serial Number"
        pattern: "^[A-F0-9]{16}$"
      homeId:
        type: "string"
        description: "HomeId"
        pattern: "^[A-Z0-9]{7}$"
      lineId:
        type: "string"
        description: "LineId"
      ontState:
        type: "string"
        description: "OntState"
    description: "Encapsulate information about created ont"
  OntResource:
    type: "object"
    properties:
      cplId:
        type: "string"
        description: "CPL_ID"
      lineId:
        type: "string"
        description: "LineId"
    description: "Encapsulate information about created ont"
  OntRollbackResult:
    type: "object"
    properties:
      error:
        description: "Result of fail operation."
        $ref: "#/definitions/CallbackError"
      response:
        type: "object"
        description: "Result of successful operation."
      success:
        type: "boolean"
    description: "OntRollbackResult"
  OntRollbackResultV2:
    type: "object"
    properties:
      error:
        description: "Result of fail operation."
        $ref: "#/definitions/CallbackError"
      response:
        type: "object"
        description: "Result of successful operation."
      success:
        type: "boolean"
    description: "OntRollbackResultV2"
  OntStateString:
    type: "object"
    properties:
      ontState:
        type: "string"
        description: "Ont State"
    description: "OntStateString"
  OntTestDto:
    type: "object"
    properties:
      ontId:
        type: "integer"
        format: "int32"
        description: "ID of the ONT in the PON"
      actualRunState:
        type: "string"
        example: "Online"
        description: "Actual status of the ONT"
        enum:
        - "OFFLINE"
        - "ONLINE"
        - "UNKNOWN"
      lastDownTime:
        type: "string"
        format: "date-time"
        example: "2018-08-21T17:32:28.0000000+00:00"
        description: "Time when the ONT last went down"
      lastDownCause:
        type: "string"
        example: "LOS"
        description: "Reason why the ONT last went down"
      lastUpTime:
        type: "string"
        format: "date-time"
        example: "2018-08-21T16:58:08.0000000+00:00"
        description: "ONT last up time"
  ProcessDto:
    type: "object"
    properties:
      source:
        type: "string"
      id:
        type: "string"
        format: "uuid"
      businessKey:
        type: "string"
      index:
        type: "integer"
        format: "int32"
  ReserveLineByHomeIdResult:
    type: "object"
    properties:
      error:
        description: "Result of fail operation."
        $ref: "#/definitions/CallbackError"
      response:
        description: "Result of successful operation."
        $ref: "#/definitions/LineIdAndCplIdDto"
      success:
        type: "boolean"
    description: "ReserveLineByHomeIdResult"
  ReserveLineByHomeIdResultV2:
    type: "object"
    properties:
      error:
        description: "Result of fail operation."
        $ref: "#/definitions/CallbackError"
      response:
        description: "Result of successful operation."
        $ref: "#/definitions/LineIdAndCplIdDto"
      success:
        type: "boolean"
    description: "ReserveLineByHomeIdResultV2"
  SearchAccessLineInformationDto:
    type: "object"
    properties:
      homeId:
        type: "string"
        description: "Criteria for search Access Line information by home id"
        pattern: "^[A-Z0-9]{7}$"
      ontSerialNumber:
        type: "string"
        description: "Criteria for search Access Line information by ont serial number"
        pattern: "^[A-F0-9]{16}$"
      subscriberNeStates:
        type: "array"
        description: "Criteria for search Access Line information by subscriber NE\
          \ profile state"
        items:
          type: "string"
          enum:
          - "INACTIVE"
          - "ACTIVE"
      portNumber:
        type: "string"
        description: "Criteria for search Access Line information by port number"
        minLength: 0
        maxLength: 50
      slotNumber:
        type: "string"
        description: "Criteria for search Access Line information by slot number"
        minLength: 0
        maxLength: 50
      endSz:
        type: "string"
        description: "Criteria for search Access Line information by endsz"
        pattern: "[0-9]{1,6}/[0-9]{1,6}/[0-9]{1,6}/[a-zA-Z0-9]{2,4}"
