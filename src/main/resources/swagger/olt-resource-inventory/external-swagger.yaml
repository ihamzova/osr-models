---
swagger: "2.0"
info:
  description: "## OLT Resource Inventory API\n\nGET/api/oltResourceInventory/v1/assurance/accessLine\
    \ \n|    Name      |    Type      |    Description      |    M/O      |    Validation\
    \ pattern  |\n| -----------------------  |---------------  |-------------------------------------\
    \  |-----------  |--------------------------------------------------------------------------------------------------------------------------------------------------------\
    \  |\n|    lineId      |    string      |           |    O      |    Valid Pattern:\
    \   [A-Z]{3}.[A-Z]{6}.[A-Z0-9]{10}   <country-code>{3}.<carrier-code>{1..6}.<line-code>{1..10}\
    \      (Sample:   DEU.DTAG.1A2B3C4E)      |\n|    ontId      |    integer    \
    \  |           |    O      |           | |\n|    ontSerialNumber      |    string\
    \      |           |    O      |           ||\n|    technology      |    string\
    \      |    Available values: GFAST, GPON      |    O      |    |\nGET/api/oltResourceInventory/v1/assurance/accessLine/{id}\
    \ \n|    Name      |    Type      |    Description      |    M/O      |    Validation\
    \ pattern  |\n| -----------------------  |---------------  |-------------------------------------\
    \  |-----------  |--------------------------------------------------------------------------------------------------------------------------------------------------------\
    \  |\n|    id     |    integer     |           |    M      |       |\nGET/api/oltResourceInventory/v1/assurance/device\
    \ \n|    Name      |    Type      |    Description      |    M/O      |    Validation\
    \ pattern  |\n| -----------------------  |---------------  |-------------------------------------\
    \  |-----------  |--------------------------------------------------------------------------------------------------------------------------------------------------------\
    \  |\n|    devicetype     |    string      |  Available values: OLT, BNG    |\
    \    O      |          |\n|    emsName      |    string      |           |   \
    \ O      |           | |\n|    endsz      |    string      |           |   O \
    \     |      minLength = 8 maxLength = 25 Valid Pattern: [0-9]{1,6}/[0-9]{1,6}/[0-9]{1,6}/[a-zA-Z0-9]{2,4}||\n\
    |   fsz      |    string      |          |    O      | Length = 4 Valid Pattern:\
    \ 76[HZ][0-9A-Z]   |\n|    vpsz      |    string      |           |  O      |\
    \          ||\nGET/api/oltResourceInventory/v1/assurance/device/{deviceId} \n\
    |    Name      |    Type      |    Description      |    M/O      |    Validation\
    \ pattern  |\n| -----------------------  |---------------  |-------------------------------------\
    \  |-----------  |--------------------------------------------------------------------------------------------------------------------------------------------------------\
    \  |\n|    deviceId     |    integer     |           |    M      |       |\nGET/api/oltResourceInventory/v1/assurance/ethernetlink\
    \ \n|    Name      |    Type      |    Description      |    M/O      |    Validation\
    \ pattern  |\n| -----------------------  |---------------  |-------------------------------------\
    \  |-----------  |--------------------------------------------------------------------------------------------------------------------------------------------------------\
    \  |\n|    bngEndSz      |    string      |           |   either bngEndSz or oltEndSz\
    \ should be mandatory      |    minLength = 8 maxLength = 25 Valid Pattern: [0-9]{1,6}/[0-9]{1,6}/[0-9]{1,6}/[a-zA-Z0-9]{2,4}\
    \      |\n|    lbz      |    integer      |           |    O      |          \
    \ | |\n|    lsz      |    string      |     Available values: 4C1, 4C2, 4Z2  \
    \    |    O      |           ||\n|    oltEndSz      |    string      |       \
    \   |    either bngEndSz or oltEndSz should be mandatory      |  minLength = 8\
    \ maxLength = 25 Valid Pattern: [0-9]{1,6}/[0-9]{1,6}/[0-9]{1,6}/[a-zA-Z0-9]{2,4}|\n\
    GET/api/oltResourceInventory/v1/assurance/ethernetlink/{id} \n|    Name      |\
    \    Type      |    Description      |    M/O      |    Validation pattern  |\n\
    | -----------------------  |---------------  |-------------------------------------\
    \  |-----------  |--------------------------------------------------------------------------------------------------------------------------------------------------------\
    \  |\n|    id     |    integer     |           |    M      |       |\nGET/api/oltResourceInventory/v1/assurance/port\
    \ \n|    Name      |    Type      |    Description      |    M/O      |    Validation\
    \ pattern  |\n| -----------------------  |---------------  |-------------------------------------\
    \  |-----------  |--------------------------------------------------------------------------------------------------------------------------------------------------------\
    \  |\n|    name      |    string      |           |    O      |          |\n|\
    \   porttype      |    integer      |  Available values: PON, ETHERNET       \
    \  |    O      |           | |\nGET/api/oltResourceInventory/v1/assurance/port/{id}\
    \ \n|    Name      |    Type      |    Description      |    M/O      |    Validation\
    \ pattern  |\n| -----------------------  |---------------  |-------------------------------------\
    \  |-----------  |--------------------------------------------------------------------------------------------------------------------------------------------------------\
    \  |\n|    id     |    integer     |           |    M      |       |"
  version: "3.2.0"
  title: "OLT Resource Inventory"
host: "localhost:8080"
tags:
- name: "AccessLineExternalController"
- name: "AssuranceExternalController"
- name: "AssuranceExternalControllerV2"
- name: "AssuranceExternalControllerV3"
- name: "DeviceExternalController"
- name: "HomeIdExternalController"
schemes:
- "http"
paths:
  /resource-order-resource-inventory/v1/assurance/accessLine:
    get:
      tags:
      - "AssuranceExternalController"
      summary: "Find Access Line by criteria"
      description: ""
      operationId: "findAccessLinesByCriteria"
      produces:
      - "application/xml"
      parameters:
      - name: "lineId"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "ontId"
        in: "query"
        description: ""
        required: false
        type: "integer"
        format: "int32"
      - name: "ontSerialNumber"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "technology"
        in: "query"
        description: ""
        required: false
        type: "string"
        enum:
        - "GFAST"
        - "GPON"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            type: "array"
            items:
              $ref: "#/definitions/AssuranceAccessLineDto"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v1/assurance/accessLine/{id}:
    get:
      tags:
      - "AssuranceExternalController"
      summary: "Find Access Line by id"
      description: ""
      operationId: "findAccessLineById"
      produces:
      - "application/xml"
      parameters:
      - name: "id"
        in: "path"
        required: true
        type: "integer"
        format: "int64"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            $ref: "#/definitions/AssuranceAccessLineDto"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v1/assurance/card:
    get:
      tags:
      - "AssuranceExternalController"
      summary: "Find card by criteria"
      description: ""
      operationId: "findCardByCriteria"
      produces:
      - "application/xml"
      parameters:
      - name: "cardtype"
        in: "query"
        description: ""
        required: false
        type: "string"
        enum:
        - "GPON"
        - "UPLINK_CARD"
        - "MANAGEMENT_CARD"
        - "SUPPORT_MODULE"
        - "PROCESSING_BOARD"
      - name: "emsName"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "firmwareVersion"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            type: "array"
            items:
              $ref: "#/definitions/AssuranceCardDto"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v1/assurance/card/{cardId}:
    get:
      tags:
      - "AssuranceExternalController"
      summary: "Find card by id"
      description: ""
      operationId: "findCardById"
      produces:
      - "application/xml"
      parameters:
      - name: "cardId"
        in: "path"
        required: true
        type: "integer"
        format: "int64"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            $ref: "#/definitions/AssuranceCardDto"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v1/assurance/device:
    get:
      tags:
      - "AssuranceExternalController"
      summary: "Find device by criteria"
      description: ""
      operationId: "findDeviceByCriteria"
      produces:
      - "application/xml"
      parameters:
      - name: "devicetype"
        in: "query"
        description: ""
        required: false
        type: "string"
        enum:
        - "OLT"
        - "BNG"
      - name: "emsName"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "endsz"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "fsz"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "vpsz"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            type: "array"
            items:
              $ref: "#/definitions/AssuranceDeviceDto"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v1/assurance/device/{deviceId}:
    get:
      tags:
      - "AssuranceExternalController"
      summary: "Find device by id"
      description: ""
      operationId: "findDeviceById"
      produces:
      - "application/xml"
      parameters:
      - name: "deviceId"
        in: "path"
        required: true
        type: "integer"
        format: "int64"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            $ref: "#/definitions/AssuranceDeviceDto"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v1/assurance/ethernetlink:
    get:
      tags:
      - "AssuranceExternalController"
      summary: "Find uplinks by filter criteria"
      description: ""
      operationId: "findUplinksByCriteria"
      produces:
      - "application/xml"
      parameters:
      - name: "bngEndSz"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "lbz"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "lsz"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "oltEndSz"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            type: "array"
            items:
              $ref: "#/definitions/AssuranceEthernetLinkDto"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v1/assurance/ethernetlink/{id}:
    get:
      tags:
      - "AssuranceExternalController"
      summary: "Find uplink by id"
      description: ""
      operationId: "findUplinkById"
      produces:
      - "application/xml"
      parameters:
      - name: "id"
        in: "path"
        required: true
        type: "integer"
        format: "int64"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            $ref: "#/definitions/AssuranceEthernetLinkDto"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v1/assurance/port:
    get:
      tags:
      - "AssuranceExternalController"
      summary: "Find Port by Name and Type"
      description: ""
      operationId: "findPortsByCriteria"
      produces:
      - "application/xml"
      parameters:
      - name: "name"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "porttype"
        in: "query"
        description: ""
        required: false
        type: "string"
        enum:
        - "PON"
        - "ETHERNET"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            type: "array"
            items:
              $ref: "#/definitions/AssurancePortDto"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v1/assurance/port/{id}:
    get:
      tags:
      - "AssuranceExternalController"
      summary: "Find port by id"
      description: ""
      operationId: "findPortById"
      produces:
      - "application/xml"
      parameters:
      - name: "id"
        in: "path"
        required: true
        type: "integer"
        format: "int64"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            $ref: "#/definitions/AssurancePortDto"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v1/homeId:
    put:
      tags:
      - "HomeIdExternalController"
      summary: "Appends HomeIds to HomeIdPool by endSZ, slotNumber and portNumber."
      description: ""
      operationId: "addHomeIds"
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "endSz"
        in: "query"
        description: "EndSz"
        required: true
        type: "string"
        pattern: "[0-9]{1,6}\\/[0-9]{1,6}\\/[0-9]{1,6}\\/[a-zA-Z0-9]{2,4}"
      - name: "slotNumber"
        in: "query"
        description: "Slot number"
        required: true
        type: "string"
      - name: "portNumber"
        in: "query"
        description: "Port number"
        required: true
        type: "string"
      - in: "body"
        name: "body"
        description: "HomeIds list"
        required: true
        schema:
          $ref: "#/definitions/HomeIdListDto"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        201:
          description: "HomeIds were added to HomeIdPool"
        400:
          description: "Bad request"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Port not found"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v1/homeId/requestFreeHomeId:
    put:
      tags:
      - "HomeIdExternalController"
      summary: "Request a free HomeID, that is assigned to the given PON-Port."
      description: ""
      operationId: "requestFreeHomeId"
      parameters:
      - in: "body"
        name: "body"
        description: "Pon Port Reference with vpSz (for example *49/30/179*), fachSz\
          \ (for example *76H1*), slotNumber (example: *4*), portNumber (example:\
          \ *1*)"
        required: true
        schema:
          $ref: "#/definitions/PonPortReferenceDto"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "HomeId was succesfully given"
          schema:
            type: "string"
        400:
          description: "Bad request"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "There isn't free HomeId"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v1/homeId/status/assigned:
    put:
      tags:
      - "HomeIdExternalController"
      summary: "Change selected homeId status to \"Assigned\"."
      description: ""
      operationId: "assignHomeId"
      parameters:
      - in: "body"
        name: "body"
        description: "homeId"
        required: true
        schema:
          type: "string"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        204:
          description: "HomeId has assigned"
        400:
          description: "Bad request"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v1/managementAccessLine/info:
    post:
      tags:
      - "AccessLineExternalController"
      summary: "Find or Get an access line by Line Id"
      description: ""
      operationId: "getLineInfo"
      consumes:
      - "application/json;charset=UTF-8"
      produces:
      - "application/json;charset=UTF-8"
      parameters:
      - in: "body"
        name: "body"
        description: "lineId"
        required: true
        schema:
          $ref: "#/definitions/LineIdDto"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer. Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully sent"
          schema:
            $ref: "#/definitions/AccessLineDto"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        401:
          description: "Unauthorized"
        403:
          description: "Forbidden"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v1/managementAccessLine/search:
    post:
      tags:
      - "AccessLineExternalController"
      summary: "Gets AccessLines by requestManagementAccessLineDto and managementUIPage."
      description: "Gets AccessLines by input parameters for search and with additional\
        \ sort parameters."
      operationId: "search"
      consumes:
      - "application/json;charset=UTF-8"
      produces:
      - "application/json;charset=UTF-8"
      parameters:
      - in: "body"
        name: "body"
        description: "Request AccessLineViewDto"
        required: false
        schema:
          $ref: "#/definitions/RequestAccessLineViewDto"
      - name: "direction"
        in: "query"
        description: "direction"
        required: false
        type: "string"
        enum:
        - "ASC"
        - "DESC"
      - name: "page"
        in: "query"
        description: "page"
        required: false
        type: "integer"
        format: "int32"
      - name: "pageSize"
        in: "query"
        description: "pageSize"
        required: false
        type: "integer"
        format: "int32"
      - name: "sortField"
        in: "query"
        description: "sortField"
        required: false
        type: "string"
        enum:
        - "END_SZ"
        - "SLOT_NUMBER"
        - "PORT_NUMBER"
        - "LINE_ID"
        - "HOME_ID"
        - "ACCESS_LINE_STATUS"
        - "SUBSCRIBER_NE_PROFILE_STATE"
        - "SUBSCRIBER_NETWORK_LINE_PROFILE_STATE"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer. Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully received"
          schema:
            $ref: "#/definitions/PageAccessLineViewDto"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        401:
          description: "Unauthorized"
        403:
          description: "Forbidden"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v1/olt:
    get:
      tags:
      - "DeviceExternalController"
      summary: "Get Device by EndSz"
      description: ""
      operationId: "getOltByEndSZ"
      produces:
      - "application/json"
      parameters:
      - name: "endSZ"
        in: "query"
        description: "endSZ"
        required: true
        type: "string"
        pattern: "[0-9]{1,6}\\/[0-9]{1,6}\\/[0-9]{1,6}\\/[a-zA-Z0-9]{2,4}"
        allowEmptyValue: true
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            $ref: "#/definitions/Device"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
    post:
      tags:
      - "DeviceExternalController"
      summary: "Create OLT by EndSz"
      description: ""
      operationId: "createOltByEndSZ"
      produces:
      - "application/json"
      parameters:
      - name: "endSZ"
        in: "query"
        description: "endSZ"
        required: true
        type: "string"
        pattern: "[0-9]{1,6}\\/[0-9]{1,6}\\/[0-9]{1,6}\\/[a-zA-Z0-9]{2,4}"
        allowEmptyValue: true
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            $ref: "#/definitions/Device"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
        409:
          description: "Entity already exists"
          schema:
            $ref: "#/definitions/ErrorResponse"
    put:
      tags:
      - "DeviceExternalController"
      summary: "Create a OLT"
      description: ""
      operationId: "saveDevice"
      produces:
      - "application/json"
      parameters:
      - in: "body"
        name: "body"
        description: "Properties of Olt"
        required: false
        schema:
          $ref: "#/definitions/Device"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully added"
          schema:
            $ref: "#/definitions/Device"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
    delete:
      tags:
      - "DeviceExternalController"
      summary: "Delete a Device"
      description: ""
      operationId: "deleteDevice"
      produces:
      - "application/json"
      parameters:
      - name: "endSZ"
        in: "query"
        description: "Properties of Device"
        required: true
        type: "string"
        pattern: "[0-9]{1,6}\\/[0-9]{1,6}\\/[0-9]{1,6}\\/[a-zA-Z0-9]{2,4}"
        allowEmptyValue: true
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully deleted"
          schema:
            $ref: "#/definitions/Device"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
        409:
          description: "Precodnition not met"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v1/olt/findBngByOltEndSz:
    post:
      tags:
      - "DeviceExternalController"
      summary: "Find bng by olt endSz"
      description: ""
      operationId: "findBngByOltEndSz"
      parameters:
      - name: "oltEndSz"
        in: "query"
        description: "OLT EndSz"
        required: true
        type: "string"
        pattern: "[0-9]{1,6}\\/[0-9]{1,6}\\/[0-9]{1,6}\\/[a-zA-Z0-9]{2,4}"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            type: "array"
            items:
              $ref: "#/definitions/Device"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v1/olt/findDeviceByPortId:
    post:
      tags:
      - "DeviceExternalController"
      summary: "Find device by port id"
      description: ""
      operationId: "findDeviceByPortId"
      produces:
      - "application/json"
      parameters:
      - name: "portId"
        in: "query"
        description: "Port id"
        required: true
        type: "integer"
        allowEmptyValue: true
        format: "int64"
      - name: "type"
        in: "query"
        required: false
        type: "string"
        enum:
        - "OLT"
        - "BNG"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            $ref: "#/definitions/Device"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v1/olt/findEndSzByDeviceType:
    post:
      tags:
      - "DeviceExternalController"
      summary: "Find EndSz's by Device-Type"
      description: ""
      operationId: "findEndSzByDeviceType"
      parameters:
      - name: "type"
        in: "query"
        description: "Device-Type"
        required: true
        type: "string"
        enum:
        - "OLT"
        - "BNG"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            type: "array"
            items:
              type: "string"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v1/olt/findFlatOltByEndsz:
    get:
      tags:
      - "DeviceExternalController"
      summary: "Find flat OLT (without related objects) by Endsz"
      description: ""
      operationId: "findFlatOltbyEndsz"
      parameters:
      - name: "endsz"
        in: "query"
        description: "OLT EndSz"
        required: true
        type: "string"
        pattern: "[0-9]{1,6}\\/[0-9]{1,6}\\/[0-9]{1,6}\\/[a-zA-Z0-9]{2,4}"
      responses:
        200:
          description: "Successfully found"
          schema:
            $ref: "#/definitions/FlatDeviceDto"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v1/olt/findOltByLineId:
    post:
      tags:
      - "DeviceExternalController"
      summary: "Find OLT by LineId"
      description: ""
      operationId: "findOltByLineId"
      parameters:
      - in: "body"
        name: "body"
        description: "LineId"
        required: true
        schema:
          type: "string"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            $ref: "#/definitions/Device"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v1/olt/getoltbyhomeid:
    post:
      tags:
      - "DeviceExternalController"
      summary: "Get Device by Home-ID"
      description: ""
      operationId: "getoltbyhomeid"
      produces:
      - "application/json"
      parameters:
      - in: "body"
        name: "body"
        required: false
        schema:
          type: "string"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            $ref: "#/definitions/Device"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v1/olt/removeSlotFromDevice:
    post:
      tags:
      - "DeviceExternalController"
      summary: "Remove Equipmentholder by id"
      description: ""
      operationId: "removeSlotFromDevice"
      produces:
      - "application/json"
      parameters:
      - name: "equipmentHolderId"
        in: "query"
        description: "Id of the equipmentholder"
        required: true
        type: "integer"
        allowEmptyValue: true
        format: "int64"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v1/olt/updateKlsId:
    put:
      tags:
      - "DeviceExternalController"
      summary: "Update Kls Id of OLT"
      description: ""
      operationId: "updateKlsId"
      produces:
      - "application/json"
      parameters:
      - name: "endSZ"
        in: "query"
        description: "endSZ"
        required: true
        type: "string"
        pattern: "[0-9]{1,6}\\/[0-9]{1,6}\\/[0-9]{1,6}\\/[a-zA-Z0-9]{2,4}"
        allowEmptyValue: true
      - name: "klsID"
        in: "query"
        required: false
        type: "integer"
        format: "int64"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully added"
          schema:
            $ref: "#/definitions/Device"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v1/olt/vpszlookup:
    get:
      tags:
      - "DeviceExternalController"
      summary: "Returns all EndSz's based on the DeviceType and VPSZ based on EndSz\
        \ Like% search."
      description: ""
      operationId: "getOltsByEndSzSegment"
      produces:
      - "application/json"
      parameters:
      - name: "vpsz"
        in: "query"
        description: " Part of Endsz"
        required: true
        type: "string"
        pattern: "^[0-9]{1,6}\\/[0-9]{1,6}\\/[0-9]{1,6}[\\/a-zA-Z0-9]{0,5}"
      - name: "devicetype"
        in: "query"
        description: "Type of element"
        required: true
        type: "string"
        enum:
        - "OLT"
        - "BNG"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            type: "array"
            items:
              type: "string"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v2/assurance/accessLine:
    get:
      tags:
      - "AssuranceExternalControllerV2"
      summary: "Find access lines by criteria"
      description: ""
      operationId: "findAccessLinesByCriteriaV2"
      produces:
      - "application/xml"
      parameters:
      - name: "lineId"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "ontId"
        in: "query"
        description: ""
        required: false
        type: "integer"
        format: "int32"
      - name: "ontSerialNumber"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "technology"
        in: "query"
        description: ""
        required: false
        type: "string"
        enum:
        - "GFAST"
        - "GPON"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            type: "array"
            items:
              $ref: "#/definitions/AssuranceAccessLineDtoV2"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v2/assurance/accessLine/{id}:
    get:
      tags:
      - "AssuranceExternalControllerV2"
      summary: "Find accessline by id"
      description: ""
      operationId: "findAccessLineByIdV2"
      produces:
      - "application/xml"
      parameters:
      - name: "id"
        in: "path"
        required: true
        type: "integer"
        format: "int64"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            $ref: "#/definitions/AssuranceAccessLineDtoV2"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v2/assurance/card:
    get:
      tags:
      - "AssuranceExternalControllerV2"
      summary: "Find card by criteria"
      description: ""
      operationId: "findCardByCriteriaV2"
      produces:
      - "application/xml"
      parameters:
      - name: "cardtype"
        in: "query"
        description: ""
        required: false
        type: "string"
        enum:
        - "GPON"
        - "UPLINK_CARD"
        - "MANAGEMENT_CARD"
        - "SUPPORT_MODULE"
        - "PROCESSING_BOARD"
      - name: "emsName"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "firmwareVersion"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            type: "array"
            items:
              $ref: "#/definitions/AssuranceCardDtoV2"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v2/assurance/card/{cardId}:
    get:
      tags:
      - "AssuranceExternalControllerV2"
      summary: "Find card by id"
      description: ""
      operationId: "findCardByIdV2"
      produces:
      - "application/xml"
      parameters:
      - name: "cardId"
        in: "path"
        required: true
        type: "integer"
        format: "int64"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            $ref: "#/definitions/AssuranceCardDtoV2"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v2/assurance/device:
    get:
      tags:
      - "AssuranceExternalControllerV2"
      summary: "Find device by criteria"
      description: ""
      operationId: "findDeviceByCriteriaV2"
      produces:
      - "application/xml"
      parameters:
      - name: "devicetype"
        in: "query"
        description: ""
        required: false
        type: "string"
        enum:
        - "OLT"
        - "BNG"
      - name: "emsName"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "endsz"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "fsz"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "vpsz"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            type: "array"
            items:
              $ref: "#/definitions/AssuranceDeviceDtoV2"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v2/assurance/device/{deviceId}:
    get:
      tags:
      - "AssuranceExternalControllerV2"
      summary: "Find device by id"
      description: ""
      operationId: "findDeviceByIdV2"
      produces:
      - "application/xml"
      parameters:
      - name: "deviceId"
        in: "path"
        required: true
        type: "integer"
        format: "int64"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            $ref: "#/definitions/AssuranceDeviceDtoV2"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v2/assurance/ethernetlink:
    get:
      tags:
      - "AssuranceExternalControllerV2"
      summary: "Find ethernetlinks by filter criteria"
      description: ""
      operationId: "findUplinksByCriteriaV2"
      produces:
      - "application/xml"
      parameters:
      - name: "bngEndSz"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "lbz"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "lsz"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "oltEndSz"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            type: "array"
            items:
              $ref: "#/definitions/AssuranceEthernetLinkDtoV2"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v2/assurance/ethernetlink/{id}:
    get:
      tags:
      - "AssuranceExternalControllerV2"
      summary: "Find ethernetlink by id"
      description: ""
      operationId: "findUplinkByIdV2"
      produces:
      - "application/xml"
      parameters:
      - name: "id"
        in: "path"
        required: true
        type: "integer"
        format: "int64"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            $ref: "#/definitions/AssuranceEthernetLinkDtoV2"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v2/assurance/port:
    get:
      tags:
      - "AssuranceExternalControllerV2"
      summary: "Find Port by Name and Type"
      description: ""
      operationId: "findPortsByCriteriaV2"
      produces:
      - "application/xml"
      parameters:
      - name: "name"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "porttype"
        in: "query"
        description: ""
        required: false
        type: "string"
        enum:
        - "PON"
        - "ETHERNET"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            type: "array"
            items:
              $ref: "#/definitions/AssurancePortDtoV2"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v2/assurance/port/{id}:
    get:
      tags:
      - "AssuranceExternalControllerV2"
      summary: "Find port by id"
      description: ""
      operationId: "findPortByIdV2"
      produces:
      - "application/xml"
      parameters:
      - name: "id"
        in: "path"
        required: true
        type: "integer"
        format: "int64"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            $ref: "#/definitions/AssurancePortDtoV2"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v3/assurance/card:
    get:
      tags:
      - "AssuranceExternalControllerV3"
      summary: "Find card by criteria"
      description: ""
      operationId: "findCardByCriteriaV3"
      produces:
      - "application/xml"
      parameters:
      - name: "cardtype"
        in: "query"
        description: ""
        required: false
        type: "string"
        enum:
        - "GPON"
        - "UPLINK_CARD"
        - "MANAGEMENT_CARD"
        - "SUPPORT_MODULE"
        - "PROCESSING_BOARD"
      - name: "emsName"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "firmwareVersion"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            type: "array"
            items:
              $ref: "#/definitions/AssuranceCardDtoV3"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v3/assurance/card/{cardId}:
    get:
      tags:
      - "AssuranceExternalControllerV3"
      summary: "Find card by id"
      description: ""
      operationId: "findCardByIdV3"
      produces:
      - "application/xml"
      parameters:
      - name: "cardId"
        in: "path"
        required: true
        type: "integer"
        format: "int64"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            $ref: "#/definitions/AssuranceCardDtoV3"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v3/assurance/device:
    get:
      tags:
      - "AssuranceExternalControllerV3"
      summary: "Find device by criteria"
      description: ""
      operationId: "findDeviceByCriteriaV3"
      produces:
      - "application/xml"
      parameters:
      - name: "devicetype"
        in: "query"
        description: ""
        required: false
        type: "string"
        enum:
        - "OLT"
        - "BNG"
      - name: "emsName"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "endsz"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "fsz"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "vpsz"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            type: "array"
            items:
              $ref: "#/definitions/AssuranceDeviceDtoV3"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v3/assurance/device/{deviceId}:
    get:
      tags:
      - "AssuranceExternalControllerV3"
      summary: "Find device by id"
      description: ""
      operationId: "findDeviceByIdV3"
      produces:
      - "application/xml"
      parameters:
      - name: "deviceId"
        in: "path"
        required: true
        type: "integer"
        format: "int64"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            $ref: "#/definitions/AssuranceDeviceDtoV3"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v3/assurance/ethernetlink:
    get:
      tags:
      - "AssuranceExternalControllerV3"
      summary: "Find ethernetlinks by filter criteria"
      description: ""
      operationId: "findUplinksByCriteriaV3"
      produces:
      - "application/xml"
      parameters:
      - name: "bngEndSz"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "lbz"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "lsz"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "oltEndSz"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            type: "array"
            items:
              $ref: "#/definitions/AssuranceEthernetLinkDtoV3"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v3/assurance/ethernetlink/{id}:
    get:
      tags:
      - "AssuranceExternalControllerV3"
      summary: "Find ethernetlink by id"
      description: ""
      operationId: "findUplinkByIdV3"
      produces:
      - "application/xml"
      parameters:
      - name: "id"
        in: "path"
        required: true
        type: "integer"
        format: "int64"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            $ref: "#/definitions/AssuranceEthernetLinkDtoV3"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v3/assurance/port:
    get:
      tags:
      - "AssuranceExternalControllerV3"
      summary: "Find port by endSz, slot and port name"
      description: ""
      operationId: "findPortsByCriteriaV3"
      produces:
      - "application/xml"
      parameters:
      - name: "deviceEndSz"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "name"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "slotNumber"
        in: "query"
        description: ""
        required: false
        type: "string"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            type: "array"
            items:
              $ref: "#/definitions/AssurancePortDtoV3"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /resource-order-resource-inventory/v3/assurance/port/{id}:
    get:
      tags:
      - "AssuranceExternalControllerV3"
      summary: "Find port by id"
      description: ""
      operationId: "findPortByIdV3"
      produces:
      - "application/xml"
      parameters:
      - name: "id"
        in: "path"
        required: true
        type: "integer"
        format: "int64"
      - name: "X-Business-Context"
        in: "header"
        description: "The business context is a general classification for a larger\
          \ number of requests (types) usually set by the originating consumer. Any\
          \ service receiving this header parameter MUST forward it unmodified to\
          \ other services it invokes. Non-REST technologies (e.g. SOABP) might use\
          \ a different identifier for this attribute."
        required: false
        type: "string"
      - name: "X-Correlation-ID"
        in: "header"
        description: "Value for identifying a single transaction across multiple services\
          \ up to the backend. Usually set by the originating consumer.Any service\
          \ receiving this header parameter MUST forward it unmodified to other services\
          \ it invokes. Non-REST technologies (e.g. SOABP) might use a different identifier\
          \ for this attribute. If this ID is not available (not supplied, empty or\
          \ at origin), the service MUST create it as a UUID. The maximum length of\
          \ 256 characters MUST NOT be exceeded (a standard UUID has 36 characters).\
          \ The creator MAY prefix the UUID with an application and/or instance defined\
          \ string."
        required: false
        type: "string"
      - name: "X-Request-ID"
        in: "header"
        description: "Value to identify an individual request across consuming application,\
          \ middleware and service provider. This ID is to be generated for every\
          \ single request and not to be passed down (as X-Correlation-ID) to sub-services.\
          \ Every consuming application MUST create this value as UUID. The maximum\
          \ length of 256 characters MUST NOT be exceeded (a standard UUID has 36\
          \ characters). The creator MAY prefix the UUID with an application and/or\
          \ instance defined string."
        required: false
        type: "string"
      responses:
        200:
          description: "Successfully found"
          schema:
            $ref: "#/definitions/AssurancePortDtoV3"
        400:
          description: "Can't perform operation"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Not present in database"
          schema:
            $ref: "#/definitions/ErrorResponse"
definitions:
  AccessLine:
    type: "object"
    required:
    - "lineId"
    properties:
      id:
        type: "integer"
        format: "int64"
      resourceId:
        type: "string"
      technology:
        type: "string"
        enum:
        - "GFAST"
        - "GPON"
      status:
        type: "string"
        enum:
        - "INACTIVE"
        - "WALLED_GARDEN"
        - "ASSIGNED"
      lineId:
        type: "string"
        pattern: "[A-Z0-9]{3}\\.[A-Z0-9]{1,6}\\.[A-Z0-9]{1,10}"
      homeId:
        type: "string"
        pattern: "^[A-Z0-9]{7}$"
      version:
        type: "integer"
        format: "int64"
      defaultNeProfile:
        $ref: "#/definitions/DefaultNeProfile"
      defaultNetworkLineProfile:
        $ref: "#/definitions/DefaultNetworkLineProfile"
      compositePartyId:
        type: "integer"
        format: "int64"
  AccessLineDto:
    type: "object"
    required:
    - "accessLine"
    - "portId"
    properties:
      portId:
        type: "integer"
        format: "int64"
      accessLine:
        $ref: "#/definitions/AccessLine"
  AccessLineViewDto:
    type: "object"
    properties:
      lineId:
        type: "string"
      homeId:
        type: "string"
      endSz:
        type: "string"
      slotNumber:
        type: "string"
      portNumber:
        type: "string"
      status:
        type: "string"
        enum:
        - "INACTIVE"
        - "WALLED_GARDEN"
        - "ASSIGNED"
      subscriberNeProfileState:
        type: "string"
        enum:
        - "INACTIVE"
        - "ACTIVE"
      subscriberNetworkLineProfileState:
        type: "string"
        enum:
        - "INACTIVE"
        - "ACTIVE"
  AllocatedFttbAnptags:
    type: "object"
    properties:
      id:
        type: "integer"
        format: "int64"
      fttbAnptag:
        type: "integer"
        format: "int32"
  AllocatedFtthAnptags:
    type: "object"
    properties:
      id:
        type: "integer"
        format: "int64"
      ftthAnptag:
        type: "integer"
        format: "int32"
  AssuranceAccessLineDto:
    type: "object"
    properties:
      defaultNeProfile:
        $ref: "#/definitions/AssuranceDefaultNeProfileDto"
      defaultNetworkLineProfile:
        $ref: "#/definitions/AssuranceDefaultNetworkLineProfileDto"
      homeId:
        type: "string"
      id:
        type: "integer"
        format: "int64"
      lineId:
        type: "string"
      portId:
        type: "integer"
        format: "int64"
      status:
        type: "string"
      subscriberNeProfile:
        $ref: "#/definitions/AssuranceSubscriberNeProfileDto"
      subscriberNetworkLineProfile:
        $ref: "#/definitions/AssuranceSubscriberNetworkLineProfileDto"
      technology:
        type: "string"
  AssuranceAccessLineDtoV2:
    type: "object"
    properties:
      defaultNeProfile:
        $ref: "#/definitions/AssuranceDefaultNeProfileDto"
      defaultNetworkLineProfile:
        $ref: "#/definitions/AssuranceDefaultNetworkLineProfileDto"
      homeId:
        type: "string"
      id:
        type: "integer"
        format: "int64"
      lineId:
        type: "string"
      portId:
        type: "integer"
        format: "int64"
      status:
        type: "string"
      partyId:
        type: "integer"
        format: "int64"
      subscriberNeProfile:
        $ref: "#/definitions/AssuranceSubscriberNeProfileDto"
      subscriberNetworkLineProfile:
        $ref: "#/definitions/AssuranceSubscriberNetworkLineProfileDto"
      technology:
        type: "string"
      resourceId:
        type: "string"
  AssuranceCardDto:
    type: "object"
    properties:
      cardtype:
        type: "string"
        description: "Type of element"
        enum:
        - "GPON"
        - "UPLINK_CARD"
        - "MANAGEMENT_CARD"
        - "SUPPORT_MODULE"
        - "PROCESSING_BOARD"
      description:
        type: "string"
        description: "Description of element"
      deviceId:
        type: "integer"
        format: "int64"
        description: "ID of the device where this card is"
      emsName:
        type: "string"
        description: "It is the unique identifier"
      firmwareVersion:
        type: "string"
        description: "The current firmware version of element"
      id:
        type: "integer"
        format: "int64"
        description: "It is the unique identifier in database"
      materialNumber:
        type: "string"
        description: "Material number of element"
      name:
        type: "string"
        description: "Name of element"
      portIDs:
        type: "array"
        description: "List of id of port for this card"
        items:
          type: "integer"
          format: "int64"
      resourceState:
        type: "string"
        description: "Status of element"
      serialNumber:
        type: "string"
        description: "Serial number of element"
      serviceState:
        type: "string"
        description: "Service number of CARD"
      shortName:
        type: "string"
        description: "Short name of element"
      slotNumber:
        type: "string"
        description: "SlotNumber of device where this card is"
      equipmentNumber:
        type: "string"
        description: "Equipment number of this card"
  AssuranceCardDtoV2:
    type: "object"
    properties:
      cardtype:
        type: "string"
        description: "Type of element"
        enum:
        - "GPON"
        - "UPLINK_CARD"
        - "MANAGEMENT_CARD"
        - "SUPPORT_MODULE"
        - "PROCESSING_BOARD"
      description:
        type: "string"
        description: "Description of element"
      deviceId:
        type: "integer"
        format: "int64"
        description: "ID of the device where this card is"
      emsName:
        type: "string"
        description: "It is the unique identifier"
      firmwareVersion:
        type: "string"
        description: "The current firmware version of element"
      id:
        type: "integer"
        format: "int64"
        description: "It is the unique identifier in database"
      materialNumber:
        type: "string"
        description: "Material number of element"
      name:
        type: "string"
        description: "Name of element"
      portIDs:
        type: "array"
        description: "List of id of port for this card"
        items:
          type: "integer"
          format: "int64"
      resourceState:
        type: "string"
        description: "Status of element"
      serialNumber:
        type: "string"
        description: "Serial number of element"
      serviceState:
        type: "string"
        description: "Service number of CARD"
      shortName:
        type: "string"
        description: "Short name of element"
      slotNumber:
        type: "string"
        description: "SlotNumber of device where this card is"
      equipmentNumber:
        type: "string"
        description: "Equipment number of this card"
      partyId:
        type: "integer"
        format: "int64"
        description: "Composite Party ID of this card"
      ressourceId:
        type: "string"
        description: "Resource ID of this card"
  AssuranceCardDtoV3:
    type: "object"
    properties:
      cardtype:
        type: "string"
        description: "Type of element"
        enum:
        - "GPON"
        - "UPLINK_CARD"
        - "MANAGEMENT_CARD"
        - "SUPPORT_MODULE"
        - "PROCESSING_BOARD"
      description:
        type: "string"
        description: "Description of element"
      deviceId:
        type: "integer"
        format: "int64"
        description: "ID of the device where this card is"
      emsName:
        type: "string"
        description: "It is the unique identifier"
      firmwareVersion:
        type: "string"
        description: "The current firmware version of element"
      id:
        type: "integer"
        format: "int64"
        description: "It is the unique identifier in database"
      materialNumber:
        type: "string"
        description: "Material number of element"
      name:
        type: "string"
        description: "Name of element"
      portIDs:
        type: "array"
        description: "List of id of port for this card"
        items:
          type: "integer"
          format: "int64"
      resourceState:
        type: "string"
        description: "Status of element"
      serialNumber:
        type: "string"
        description: "Serial number of element"
      serviceState:
        type: "string"
        description: "Service number of CARD"
      shortName:
        type: "string"
        description: "Short name of element"
      slotNumber:
        type: "string"
        description: "SlotNumber of device where this card is"
      equipmentNumber:
        type: "string"
        description: "Equipment number of this card"
      partyId:
        type: "integer"
        format: "int64"
        description: "Composite Party ID of this card"
      ressourceId:
        type: "string"
        description: "Resource ID of this card"
  AssuranceDefaultNeProfileDto:
    type: "object"
    properties:
      ancpPartitionId:
        type: "integer"
        format: "int32"
      anpTag:
        type: "integer"
        format: "int32"
      ontId:
        type: "integer"
        format: "int32"
      ontSerialNumber:
        type: "string"
      profileName:
        type: "string"
      state:
        type: "string"
  AssuranceDefaultNetworkLineProfileDto:
    type: "object"
    properties:
      accessNetworkType:
        type: "string"
      accessType:
        type: "string"
      authMethod:
        type: "integer"
        format: "int32"
      guaranteedDownBandwidth:
        type: "integer"
        format: "int32"
      guaranteedUpBandwidth:
        type: "integer"
        format: "int32"
      klsId:
        type: "integer"
        format: "int64"
      maxDownBandwidth:
        type: "integer"
        format: "int32"
      maxUpBandwidth:
        type: "integer"
        format: "int32"
      minDownBandwidth:
        type: "integer"
        format: "int32"
      minUpBandwidth:
        type: "integer"
        format: "int32"
      state:
        type: "string"
  AssuranceDeviceDto:
    type: "object"
    properties:
      cardIDs:
        type: "array"
        description: "List of id of card for this device"
        items:
          type: "integer"
          format: "int64"
      communicationState:
        type: "string"
        description: "Communication state of device"
      description:
        type: "string"
        description: "Description of element"
      deviceName:
        type: "string"
        description: "Name of element"
      devicetype:
        type: "string"
        description: "Type of element"
        enum:
        - "OLT"
        - "BNG"
      emsName:
        type: "string"
        description: "It is the unique identifier"
      endsz:
        type: "string"
        description: "EndSz of device"
        pattern: "[0-9]{1,6}\\/[0-9]{1,6}\\/[0-9]{1,6}\\/[a-zA-Z0-9]{2,4}"
      firmwareVersion:
        type: "string"
        description: "The current firmware version of element"
      fsz:
        type: "string"
        description: "Description of the type network element and it is part of end_sz"
      id:
        type: "integer"
        format: "int64"
        description: "It is the unique identifier in database"
      importState:
        type: "string"
        description: "Import status of device"
      ipaddress:
        type: "string"
        description: "IP-Address of device"
      klsid:
        type: "string"
        description: "Id of the location of device"
      materialNumber:
        type: "string"
        description: "Material number of element"
      resourceState:
        type: "string"
        description: "Status of element"
      serialNumber:
        type: "string"
        description: "Serial number of element"
      shortName:
        type: "string"
        description: "Short name of element"
      vendor:
        type: "string"
        description: "Model of device"
      vpsz:
        type: "string"
        description: " Part of Endsz"
      asb:
        type: "string"
        description: "ASB of device"
      technicalLocation:
        type: "string"
        description: "Technical location of device"
      equipmentNumber:
        type: "string"
        description: "Equipment number of device"
  AssuranceDeviceDtoV2:
    type: "object"
    properties:
      cardIDs:
        type: "array"
        description: "List of id of card for this device"
        items:
          type: "integer"
          format: "int64"
      communicationState:
        type: "string"
        description: "Communication state of device"
      description:
        type: "string"
        description: "Description of element"
      deviceName:
        type: "string"
        description: "Name of element"
      devicetype:
        type: "string"
        description: "Type of element"
        enum:
        - "OLT"
        - "BNG"
      emsName:
        type: "string"
        description: "It is the unique identifier"
      endsz:
        type: "string"
        description: "EndSz of device"
        pattern: "[0-9]{1,6}\\/[0-9]{1,6}\\/[0-9]{1,6}\\/[a-zA-Z0-9]{2,4}"
      firmwareVersion:
        type: "string"
        description: "The current firmware version of element"
      fsz:
        type: "string"
        description: "Description of the type network element and it is part of end_sz"
      id:
        type: "integer"
        format: "int64"
        description: "It is the unique identifier in database"
      importState:
        type: "string"
        description: "Import status of device"
      ipaddress:
        type: "string"
        description: "IP-Address of device"
      klsid:
        type: "string"
        description: "Id of the location of device"
      materialNumber:
        type: "string"
        description: "Material number of element"
      resourceState:
        type: "string"
        description: "Status of element"
      serialNumber:
        type: "string"
        description: "Serial number of element"
      shortName:
        type: "string"
        description: "Short name of element"
      vendor:
        type: "string"
        description: "Model of device"
      vpsz:
        type: "string"
        description: " Part of Endsz"
      asb:
        type: "string"
        description: "ASB of device"
      technicalLocation:
        type: "string"
        description: "Technical location of device"
      equipmentNumber:
        type: "string"
        description: "Equipment number of device"
      partyId:
        type: "integer"
        format: "int64"
        description: "Composite Party ID of device"
      ressourceId:
        type: "string"
        description: "Resource ID of device"
  AssuranceDeviceDtoV3:
    type: "object"
    properties:
      cardIDs:
        type: "array"
        description: "List of id of card for this device"
        items:
          type: "integer"
          format: "int64"
      communicationState:
        type: "string"
        description: "Communication state of device"
      description:
        type: "string"
        description: "Description of element"
      deviceName:
        type: "string"
        description: "Name of element"
      devicetype:
        type: "string"
        description: "Type of element"
        enum:
        - "OLT"
        - "BNG"
      emsName:
        type: "string"
        description: "It is the unique identifier"
      endsz:
        type: "string"
        description: "EndSz of device"
        pattern: "[0-9]{1,6}\\/[0-9]{1,6}\\/[0-9]{1,6}\\/[a-zA-Z0-9]{2,4}"
      firmwareVersion:
        type: "string"
        description: "The current firmware version of element"
      fsz:
        type: "string"
        description: "Description of the type network element and it is part of end_sz"
      id:
        type: "integer"
        format: "int64"
        description: "It is the unique identifier in database"
      importState:
        type: "string"
        description: "Import status of device"
      ipaddress:
        type: "string"
        description: "IP-Address of device"
      klsid:
        type: "string"
        description: "Id of the location of device"
      materialNumber:
        type: "string"
        description: "Material number of element"
      resourceState:
        type: "string"
        description: "Status of element"
      serialNumber:
        type: "string"
        description: "Serial number of element"
      shortName:
        type: "string"
        description: "Short name of element"
      vendor:
        type: "string"
        description: "Model of device"
      vpsz:
        type: "string"
        description: " Part of Endsz"
      asb:
        type: "string"
        description: "ASB of device"
      technicalLocation:
        type: "string"
        description: "Technical location of device"
      equipmentNumber:
        type: "string"
        description: "Equipment number of device"
      partyId:
        type: "integer"
        format: "int64"
        description: "Composite Party ID of device"
      ressourceId:
        type: "string"
        description: "Resource ID of device"
  AssuranceEthernetLinkDto:
    type: "object"
    properties:
      ancpStatus:
        type: "string"
        description: "Ancp status of ethernet link"
        enum:
        - "ACTIVE"
        - "INACTIVE"
      bngPortRef:
        type: "integer"
        format: "int64"
        description: "ID of the BNG PORT where this port is"
      endsza:
        type: "string"
        description: "EndSz of device"
        pattern: "[0-9]{1,6}\\/[0-9]{1,6}\\/[0-9]{1,6}\\/[a-zA-Z0-9]{2,4}"
      endszb:
        type: "string"
        description: "EndSz of device"
        pattern: "[0-9]{1,6}\\/[0-9]{1,6}\\/[0-9]{1,6}\\/[a-zA-Z0-9]{2,4}"
      id:
        type: "integer"
        format: "int64"
        description: "It is the unique identifier in database"
      ipStatus:
        type: "string"
        description: "Status of ethernet link"
        enum:
        - "ACTIVE"
        - "PLANNED"
        - "ACTIVATION_PROCESS"
      lbz:
        type: "string"
        description: "LBZ"
      lsz:
        type: "string"
        description: "Business description of link between OLT and BNG"
      oltPortRef:
        type: "integer"
        format: "int64"
        description: "ID of the OLT PORT where this port is"
      ordnungsnummer:
        type: "integer"
        format: "int32"
        description: "OrderNumber is an identifier of link"
      uewegId:
        type: "string"
        description: "UEWEGID is an identifier of link"
  AssuranceEthernetLinkDtoV2:
    type: "object"
    properties:
      ancpStatus:
        type: "string"
        description: "Ancp status of ethernet link"
        enum:
        - "ACTIVE"
        - "INACTIVE"
      bngPortRef:
        type: "integer"
        format: "int64"
        description: "ID of the BNG PORT where this port is"
      endsza:
        type: "string"
        description: "EndSz of device"
        pattern: "[0-9]{1,6}\\/[0-9]{1,6}\\/[0-9]{1,6}\\/[a-zA-Z0-9]{2,4}"
      endszb:
        type: "string"
        description: "EndSz of device"
        pattern: "[0-9]{1,6}\\/[0-9]{1,6}\\/[0-9]{1,6}\\/[a-zA-Z0-9]{2,4}"
      id:
        type: "integer"
        format: "int64"
        description: "It is the unique identifier in database"
      ipStatus:
        type: "string"
        description: "Status of ethernet link"
        enum:
        - "ACTIVE"
        - "PLANNED"
        - "ACTIVATION_PROCESS"
      lbz:
        type: "string"
        description: "LBZ"
      lsz:
        type: "string"
        description: "Business description of link between OLT and BNG"
      oltPortRef:
        type: "integer"
        format: "int64"
        description: "ID of the OLT PORT where this port is"
      ordnungsnummer:
        type: "integer"
        format: "int32"
        description: "OrderNumber is an identifier of link"
      uewegId:
        type: "string"
        description: "UEWEGID is an identifier of link"
      partyId:
        type: "integer"
        format: "int64"
        description: "Composite Party ID of this ethernetlink"
      resourceId:
        type: "string"
        description: "Resource ID of this ethernetlink"
  AssuranceEthernetLinkDtoV3:
    type: "object"
    properties:
      ancpStatus:
        type: "string"
        description: "Ancp status of ethernet link"
        enum:
        - "ACTIVE"
        - "INACTIVE"
      bngPortRef:
        type: "integer"
        format: "int64"
        description: "ID of the BNG PORT where this port is"
      endsza:
        type: "string"
        description: "EndSz of device"
        pattern: "[0-9]{1,6}\\/[0-9]{1,6}\\/[0-9]{1,6}\\/[a-zA-Z0-9]{2,4}"
      endszb:
        type: "string"
        description: "EndSz of device"
        pattern: "[0-9]{1,6}\\/[0-9]{1,6}\\/[0-9]{1,6}\\/[a-zA-Z0-9]{2,4}"
      id:
        type: "integer"
        format: "int64"
        description: "It is the unique identifier in database"
      ipStatus:
        type: "string"
        description: "Status of ethernet link"
        enum:
        - "ACTIVE"
        - "PLANNED"
        - "ACTIVATION_PROCESS"
      lbz:
        type: "string"
        description: "LBZ"
      lsz:
        type: "string"
        description: "Business description of link between OLT and BNG"
      oltPortRef:
        type: "integer"
        format: "int64"
        description: "ID of the OLT PORT where this port is"
      ordnungsnummer:
        type: "integer"
        format: "int32"
        description: "OrderNumber is an identifier of link"
      uewegId:
        type: "string"
        description: "UEWEGID is an identifier of link"
      partyId:
        type: "integer"
        format: "int64"
        description: "Composite Party ID of this ethernetlink"
      resourceId:
        type: "string"
        description: "Resource ID of this ethernetlink"
  AssuranceOpticalModuleDto:
    type: "object"
    properties:
      description:
        type: "string"
      emsName:
        type: "string"
      manufacturer:
        type: "string"
      materialNumber:
        type: "string"
      name:
        type: "string"
      shortName:
        type: "string"
  AssuranceOpticalModuleDtoV2:
    type: "object"
    properties:
      description:
        type: "string"
      emsName:
        type: "string"
      manufacturer:
        type: "string"
      materialNumber:
        type: "string"
      name:
        type: "string"
      shortName:
        type: "string"
      partyId:
        type: "integer"
        format: "int64"
      resourceId:
        type: "string"
  AssuranceOpticalModuleDtoV3:
    type: "object"
    properties:
      description:
        type: "string"
      emsName:
        type: "string"
      manufacturer:
        type: "string"
      materialNumber:
        type: "string"
      name:
        type: "string"
      shortName:
        type: "string"
      partyId:
        type: "integer"
        format: "int64"
      resourceId:
        type: "string"
  AssurancePortDto:
    type: "object"
    properties:
      accessLineIDs:
        type: "array"
        description: "List of id of accesses line for this port"
        items:
          type: "integer"
          format: "int64"
      cardId:
        type: "integer"
        format: "int64"
        description: "ID of the card where this port is"
      ethernetLinkIDs:
        type: "array"
        description: "List of id of ethernet link for this port"
        items:
          type: "integer"
          format: "int64"
      id:
        type: "integer"
        format: "int64"
        description: "It is the unique identifier in database"
      opticalModule:
        description: "Service number of CARD"
        $ref: "#/definitions/AssuranceOpticalModuleDto"
      portNumber:
        type: "string"
        description: "It is the unique identifier in database"
      porttype:
        type: "string"
        description: "Type of element"
        enum:
        - "PON"
        - "ETHERNET"
  AssurancePortDtoV2:
    type: "object"
    properties:
      accessLineIDs:
        type: "array"
        description: "List of id of accesses line for this port"
        items:
          type: "integer"
          format: "int64"
      cardId:
        type: "integer"
        format: "int64"
        description: "ID of the card where this port is"
      ethernetLinkIDs:
        type: "array"
        description: "List of id of ethernet link for this port"
        items:
          type: "integer"
          format: "int64"
      id:
        type: "integer"
        format: "int64"
        description: "It is the unique identifier in database"
      opticalModule:
        description: "Service number of CARD"
        $ref: "#/definitions/AssuranceOpticalModuleDtoV2"
      portNumber:
        type: "string"
        description: "It is the unique identifier in database"
      porttype:
        type: "string"
        description: "Type of element"
        enum:
        - "PON"
        - "ETHERNET"
      partyId:
        type: "integer"
        format: "int64"
        description: "Composite Party ID of this port"
      ressourceId:
        type: "string"
        description: "Resource ID of this port"
  AssurancePortDtoV3:
    type: "object"
    properties:
      cardId:
        type: "integer"
        format: "int64"
        description: "ID of the card where this port is"
      ethernetLinkIDs:
        type: "array"
        description: "List of id of ethernet link for this port"
        items:
          type: "integer"
          format: "int64"
      id:
        type: "integer"
        format: "int64"
        description: "It is the unique identifier in database"
      opticalModule:
        description: "Service number of CARD"
        $ref: "#/definitions/AssuranceOpticalModuleDtoV3"
      portNumber:
        type: "string"
        description: "It is the unique identifier in database"
      porttype:
        type: "string"
        description: "Type of element"
        enum:
        - "PON"
        - "ETHERNET"
      partyId:
        type: "integer"
        format: "int64"
        description: "Composite Party ID of this port"
      ressourceId:
        type: "string"
        description: "Resource ID of this port"
  AssuranceSubscriberNeProfileDto:
    type: "object"
    properties:
      ontSerialNumber:
        type: "string"
      profileName:
        type: "string"
  AssuranceSubscriberNetworkLineProfileDto:
    type: "object"
    properties:
      authMethod:
        type: "integer"
        format: "int32"
      downBandwidth:
        type: "integer"
        format: "int32"
      federationPartner:
        type: "string"
      guaranteedDownBandwidth:
        type: "integer"
        format: "int32"
      guaranteedUpBandwidth:
        type: "integer"
        format: "int32"
      klsId:
        type: "integer"
        format: "int64"
      l2ccid:
        type: "string"
      maxDownBandwidth:
        type: "integer"
        format: "int32"
      maxUpBandwidth:
        type: "integer"
        format: "int32"
      minDownBandwidth:
        type: "integer"
        format: "int32"
      minUpBandwidth:
        type: "integer"
        format: "int32"
      providerId:
        type: "string"
      substitutionDomain:
        type: "string"
      upBandwidth:
        type: "integer"
        format: "int32"
  Card:
    type: "object"
    properties:
      shortName:
        type: "string"
      materialNumber:
        type: "string"
      emsNbiName:
        type: "string"
      tkz1:
        type: "string"
      tkz2:
        type: "string"
      descriptionUrl:
        type: "string"
      lastDiscovery:
        type: "string"
        format: "date-time"
      id:
        type: "integer"
        format: "int64"
      resourceId:
        type: "string"
      name:
        type: "string"
      description:
        type: "string"
      serialNumber:
        type: "string"
      serviceState:
        type: "string"
      resourceState:
        type: "string"
      firmwareVersion:
        type: "string"
      cardType:
        type: "string"
        enum:
        - "GPON"
        - "UPLINK_CARD"
        - "MANAGEMENT_CARD"
        - "SUPPORT_MODULE"
        - "PROCESSING_BOARD"
      equipmentNumber:
        type: "string"
      manufacturer:
        type: "string"
      ports:
        type: "array"
        items:
          $ref: "#/definitions/Port"
      compositePartyId:
        type: "integer"
        format: "int64"
  DefaultNeProfile:
    type: "object"
    properties:
      profileName:
        type: "string"
        minLength: 0
        maxLength: 50
      ontSerialNumber:
        type: "string"
        minLength: 0
        maxLength: 50
      state:
        type: "string"
        enum:
        - "INACTIVE"
        - "ACTIVE"
      version:
        type: "integer"
        format: "int64"
      id:
        type: "integer"
        format: "int64"
      ancpPartitionId:
        type: "integer"
        format: "int32"
        minimum: 0
        maximum: 255
      ontId:
        type: "integer"
        format: "int32"
        minimum: 0
        maximum: 128
      ontState:
        type: "string"
        enum:
        - "OFFLINE"
        - "ONLINE"
        - "UNKNOWN"
      anpTag:
        type: "integer"
        format: "int32"
      subscriberNeProfile:
        $ref: "#/definitions/SubscriberNeProfile"
  DefaultNetworkLineProfile:
    type: "object"
    properties:
      authMethod:
        type: "integer"
        format: "int32"
        minimum: 0
        maximum: 1
      klsId:
        type: "integer"
        format: "int64"
      minDownBandwidth:
        type: "integer"
        format: "int32"
      minUpBandwidth:
        type: "integer"
        format: "int32"
      guaranteedDownBandwidth:
        type: "integer"
        format: "int32"
      guaranteedUpBandwidth:
        type: "integer"
        format: "int32"
      maxDownBandwidth:
        type: "integer"
        format: "int32"
      maxUpBandwidth:
        type: "integer"
        format: "int32"
      state:
        type: "string"
        enum:
        - "INACTIVE"
        - "ACTIVE"
      federationPartner:
        type: "string"
      version:
        type: "integer"
        format: "int64"
      id:
        type: "integer"
        format: "int64"
      accessNetworkType:
        type: "string"
      accessType:
        type: "string"
      subscriberNetworkLineProfile:
        $ref: "#/definitions/SubscriberNetworkLineProfile"
  Device:
    type: "object"
    properties:
      shortName:
        type: "string"
      materialNumber:
        type: "string"
      emsNbiName:
        type: "string"
      tkz1:
        type: "string"
      tkz2:
        type: "string"
      descriptionUrl:
        type: "string"
      lastDiscovery:
        type: "string"
        format: "date-time"
      id:
        type: "integer"
        format: "int64"
      resourceId:
        type: "string"
      deviceName:
        type: "string"
      endSz:
        type: "string"
        pattern: "[0-9]{1,6}\\/[0-9]{1,6}\\/[0-9]{1,6}\\/[a-zA-Z0-9]{2,4}"
      fachSz:
        type: "string"
        pattern: "76[HZ][0-9A-Z]"
      klsId:
        type: "integer"
        format: "int64"
      ipAddress:
        type: "string"
      firmwareVersion:
        type: "string"
      resourceState:
        type: "string"
        enum:
        - "NON_WORKING"
        - "WORKING"
      communicationState:
        type: "string"
        enum:
        - "CS_AVAILABLE"
        - "CS_UNAVAILABLE"
      serialNumber:
        type: "string"
      importState:
        type: "string"
        enum:
        - "PLANNED"
        - "DISCOVERED"
      type:
        type: "string"
        enum:
        - "OLT"
        - "BNG"
      importDate:
        type: "string"
        format: "date-time"
      equipmentHolders:
        type: "array"
        items:
          $ref: "#/definitions/EquipmentHolder"
      ftthAnptagRange:
        $ref: "#/definitions/FtthAnptagRange"
      fttbAnptagRange:
        $ref: "#/definitions/FttbAnptagRange"
      description:
        type: "string"
      asb:
        type: "integer"
        format: "int64"
      technicalLocation:
        type: "string"
      equipmentNumber:
        type: "string"
      manufacturer:
        type: "string"
      compositePartyId:
        type: "integer"
        format: "int64"
  EquipmentHolder:
    type: "object"
    properties:
      id:
        type: "integer"
        format: "int64"
      slotNumber:
        type: "string"
      card:
        $ref: "#/definitions/Card"
  ErrorResponse:
    type: "object"
    properties:
      errorCode:
        type: "string"
        description: "Error code"
      message:
        type: "string"
        description: "Message"
    description: "Error response of 4xx and 5xx http statuses"
  FlatDeviceDto:
    type: "object"
    properties:
      shortName:
        type: "string"
      materialNumber:
        type: "string"
      emsNbiName:
        type: "string"
      tkz1:
        type: "string"
      tkz2:
        type: "string"
      descriptionUrl:
        type: "string"
      lastDiscovery:
        type: "string"
        format: "date-time"
      id:
        type: "integer"
        format: "int64"
      deviceName:
        type: "string"
      endSz:
        type: "string"
        pattern: "[0-9]{1,6}\\/[0-9]{1,6}\\/[0-9]{1,6}\\/[a-zA-Z0-9]{2,4}"
      fachSz:
        type: "string"
        pattern: "76[HZ][0-9A-Z]"
      klsId:
        type: "integer"
        format: "int64"
      ipAddress:
        type: "string"
      firmwareVersion:
        type: "string"
      resourceState:
        type: "string"
        enum:
        - "NON_WORKING"
        - "WORKING"
      communicationState:
        type: "string"
        enum:
        - "CS_AVAILABLE"
        - "CS_UNAVAILABLE"
      serialNumber:
        type: "string"
      importState:
        type: "string"
        enum:
        - "PLANNED"
        - "DISCOVERED"
      type:
        type: "string"
        enum:
        - "OLT"
        - "BNG"
      importDate:
        type: "string"
        format: "date-time"
      description:
        type: "string"
      asb:
        type: "integer"
        format: "int64"
      technicalLocation:
        type: "string"
      equipmentNumber:
        type: "string"
      manufacturer:
        type: "string"
      compositePartyId:
        type: "integer"
        format: "int64"
  FttbAnptagRange:
    type: "object"
    properties:
      id:
        type: "integer"
        format: "int64"
      endWith:
        type: "integer"
        format: "int32"
      startFrom:
        type: "integer"
        format: "int32"
      allocatedFttbTags:
        type: "array"
        items:
          $ref: "#/definitions/AllocatedFttbAnptags"
  FtthAnptagRange:
    type: "object"
    properties:
      id:
        type: "integer"
        format: "int64"
      startFrom:
        type: "integer"
        format: "int32"
      endWith:
        type: "integer"
        format: "int32"
      allocatedFtthTags:
        type: "array"
        items:
          $ref: "#/definitions/AllocatedFtthAnptags"
  HomeIdListDto:
    type: "object"
    properties:
      homeIdList:
        type: "array"
        items:
          type: "string"
  HomeIdPool:
    type: "object"
    properties:
      id:
        type: "integer"
        format: "int64"
      homeId:
        type: "string"
      homeIdStatus:
        type: "string"
        enum:
        - "FREE"
        - "RESERVED"
        - "ASSIGNED"
  LineIdDto:
    type: "object"
    required:
    - "lineId"
    properties:
      lineId:
        type: "string"
        pattern: "[A-Z0-9]{3}\\.[A-Z0-9]{1,6}\\.[A-Z0-9]{1,10}"
  LineIdPool:
    type: "object"
    properties:
      id:
        type: "integer"
        format: "int64"
      lineId:
        type: "string"
      status:
        type: "string"
  OpticalModule:
    type: "object"
    properties:
      shortName:
        type: "string"
      materialNumber:
        type: "string"
      emsNbiName:
        type: "string"
      tkz1:
        type: "string"
      tkz2:
        type: "string"
      descriptionUrl:
        type: "string"
      lastDiscovery:
        type: "string"
        format: "date-time"
      id:
        type: "integer"
        format: "int64"
      resourceId:
        type: "string"
      name:
        type: "string"
      description:
        type: "string"
      manufacturer:
        type: "string"
      compositePartyId:
        type: "integer"
        format: "int64"
  Page:
    type: "object"
    properties:
      totalElements:
        type: "integer"
        format: "int64"
      totalPages:
        type: "integer"
        format: "int32"
      size:
        type: "integer"
        format: "int32"
      content:
        type: "array"
        items:
          type: "object"
      number:
        type: "integer"
        format: "int32"
      sort:
        $ref: "#/definitions/Sort"
      last:
        type: "boolean"
      pageable:
        $ref: "#/definitions/Pageable"
      first:
        type: "boolean"
      numberOfElements:
        type: "integer"
        format: "int32"
  PageAccessLineViewDto:
    type: "object"
    properties:
      totalElements:
        type: "integer"
        format: "int64"
      totalPages:
        type: "integer"
        format: "int32"
      size:
        type: "integer"
        format: "int32"
      content:
        type: "array"
        items:
          $ref: "#/definitions/AccessLineViewDto"
      number:
        type: "integer"
        format: "int32"
      sort:
        $ref: "#/definitions/Sort"
      last:
        type: "boolean"
      pageable:
        $ref: "#/definitions/Pageable"
      first:
        type: "boolean"
      numberOfElements:
        type: "integer"
        format: "int32"
  Pageable:
    type: "object"
    properties:
      offset:
        type: "integer"
        format: "int64"
      sort:
        $ref: "#/definitions/Sort"
      pageSize:
        type: "integer"
        format: "int32"
      pageNumber:
        type: "integer"
        format: "int32"
      unpaged:
        type: "boolean"
      paged:
        type: "boolean"
  PonPortReferenceDto:
    type: "object"
    properties:
      vpSz:
        type: "string"
      fachSz:
        type: "string"
      slotNumber:
        type: "string"
      portNumber:
        type: "string"
  Port:
    type: "object"
    properties:
      id:
        type: "integer"
        format: "int64"
      resourceId:
        type: "string"
      portType:
        type: "string"
        enum:
        - "PON"
        - "ETHERNET"
      portNumber:
        type: "string"
      opticalModule:
        $ref: "#/definitions/OpticalModule"
      homeIdPools:
        type: "array"
        items:
          $ref: "#/definitions/HomeIdPool"
      lineIdPools:
        type: "array"
        items:
          $ref: "#/definitions/LineIdPool"
      accessLines:
        type: "array"
        items:
          $ref: "#/definitions/AccessLine"
      compositePartyId:
        type: "integer"
        format: "int64"
  RequestAccessLineViewDto:
    type: "object"
    properties:
      lineId:
        type: "string"
        pattern: "[A-Z0-9]{3}\\.[A-Z0-9]{1,6}\\.[A-Z0-9]{1,10}"
      homeId:
        type: "string"
        pattern: "^[A-Z0-9]{7}$"
      endSz:
        type: "string"
        pattern: "[0-9]{1,6}\\/[0-9]{1,6}\\/[0-9]{1,6}\\/[a-zA-Z0-9]{2,4}"
      statuses:
        type: "array"
        uniqueItems: true
        items:
          type: "string"
          enum:
          - "INACTIVE"
          - "WALLED_GARDEN"
          - "ASSIGNED"
      subscriberNeProfileState:
        type: "string"
        enum:
        - "INACTIVE"
        - "ACTIVE"
      subscriberNetworkLineProfileState:
        type: "string"
        enum:
        - "INACTIVE"
        - "ACTIVE"
      slotNumber:
        type: "string"
      portNumber:
        type: "string"
  Sort:
    type: "object"
    properties:
      sorted:
        type: "boolean"
      unsorted:
        type: "boolean"
  SubscriberNeProfile:
    type: "object"
    properties:
      profileName:
        type: "string"
        minLength: 0
        maxLength: 50
      ontSerialNumber:
        type: "string"
        minLength: 0
        maxLength: 50
      state:
        type: "string"
        enum:
        - "INACTIVE"
        - "ACTIVE"
      version:
        type: "integer"
        format: "int64"
      id:
        type: "integer"
        format: "int64"
  SubscriberNetworkLineProfile:
    type: "object"
    properties:
      authMethod:
        type: "integer"
        format: "int32"
        minimum: 0
        maximum: 1
      klsId:
        type: "integer"
        format: "int64"
      minDownBandwidth:
        type: "integer"
        format: "int32"
      minUpBandwidth:
        type: "integer"
        format: "int32"
      guaranteedDownBandwidth:
        type: "integer"
        format: "int32"
      guaranteedUpBandwidth:
        type: "integer"
        format: "int32"
      maxDownBandwidth:
        type: "integer"
        format: "int32"
      maxUpBandwidth:
        type: "integer"
        format: "int32"
      state:
        type: "string"
        enum:
        - "INACTIVE"
        - "ACTIVE"
      federationPartner:
        type: "string"
      version:
        type: "integer"
        format: "int64"
      id:
        type: "integer"
        format: "int64"
      providerId:
        type: "string"
        pattern: "^[0-9]{1,4}$"
      downBandwidth:
        type: "integer"
        format: "int32"
      upBandwidth:
        type: "integer"
        format: "int32"
      substitutionDomain:
        type: "string"
        pattern: "^([a-z0-9_\\-.]){1,32}$"
      l2ccid:
        type: "string"
        pattern: "^([a-z0-9_\\-.]){1,32}$"

